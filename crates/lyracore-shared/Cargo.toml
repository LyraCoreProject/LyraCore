[package]
name = "lyracore-shared"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
homepage.workspace = true
publish.workspace = true
description = "LyraCore: protocol-agnostic constants and helpers shared by the SpacetimeDB module and the gateway."

# Intentionally NO external dependencies: this crate is consumed both by the wasm module
# and the native gateway, so it must stay portable and trivially compilable.
[dependencies]

[lints]
workspace = true
