// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::mail_timer_type::MailTimer;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct FireMailTimerArgs {
    pub timer: MailTimer,
}

impl From<FireMailTimerArgs> for super::Reducer {
    fn from(args: FireMailTimerArgs) -> Self {
        Self::FireMailTimer { timer: args.timer }
    }
}

impl __sdk::InModule for FireMailTimerArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `fire_mail_timer`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait fire_mail_timer {
    /// Request that the remote module invoke the reducer `fire_mail_timer` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`fire_mail_timer:fire_mail_timer_then`] to run a callback after the reducer completes.
    fn fire_mail_timer(&self, timer: MailTimer) -> __sdk::Result<()> {
        self.fire_mail_timer_then(timer, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `fire_mail_timer` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn fire_mail_timer_then(
        &self,
        timer: MailTimer,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl fire_mail_timer for super::RemoteReducers {
    fn fire_mail_timer_then(
        &self,
        timer: MailTimer,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(FireMailTimerArgs { timer }, callback)
    }
}
