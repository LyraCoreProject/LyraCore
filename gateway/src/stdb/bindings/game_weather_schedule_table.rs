// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::weather_schedule_type::WeatherSchedule;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `game_weather_schedule`.
///
/// Obtain a handle from the [`GameWeatherScheduleTableAccess::game_weather_schedule`] method on [`super::RemoteTables`],
/// like `ctx.db.game_weather_schedule()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_weather_schedule().on_insert(...)`.
pub struct GameWeatherScheduleTableHandle<'ctx> {
    imp: __sdk::TableHandle<WeatherSchedule>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

/// Lifetime-aware accessor marker for the table `game_weather_schedule`.
pub struct GameWeatherScheduleTableAccessor;

impl __sdk::TableAccessor<super::RemoteTables> for GameWeatherScheduleTableAccessor {
    type Row = WeatherSchedule;
    type Handle<'db> = GameWeatherScheduleTableHandle<'db>;

    fn get<'db>(db: &'db super::RemoteTables) -> Self::Handle<'db> {
        db.game_weather_schedule()
    }
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `game_weather_schedule`.
///
/// Implemented for [`super::RemoteTables`].
pub trait GameWeatherScheduleTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`GameWeatherScheduleTableHandle`], which mediates access to the table `game_weather_schedule`.
    fn game_weather_schedule(&self) -> GameWeatherScheduleTableHandle<'_>;
}

impl GameWeatherScheduleTableAccess for super::RemoteTables {
    fn game_weather_schedule(&self) -> GameWeatherScheduleTableHandle<'_> {
        GameWeatherScheduleTableHandle {
            imp: self
                .imp
                .get_table::<WeatherSchedule>("game_weather_schedule"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct GameWeatherScheduleInsertCallbackId(__sdk::CallbackId);
pub struct GameWeatherScheduleDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableLike for GameWeatherScheduleTableHandle<'ctx> {
    type Row = WeatherSchedule;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = WeatherSchedule> + '_ {
        self.imp.iter()
    }
}

impl<'ctx> __sdk::Table for GameWeatherScheduleTableHandle<'ctx> {
    type Row = WeatherSchedule;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = WeatherSchedule> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = GameWeatherScheduleInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameWeatherScheduleInsertCallbackId {
        GameWeatherScheduleInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameWeatherScheduleInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = GameWeatherScheduleDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameWeatherScheduleDeleteCallbackId {
        GameWeatherScheduleDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameWeatherScheduleDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

impl<'ctx> __sdk::WithInsert for GameWeatherScheduleTableHandle<'ctx> {
    type InsertCallbackId = GameWeatherScheduleInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameWeatherScheduleInsertCallbackId {
        GameWeatherScheduleInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameWeatherScheduleInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }
}

impl<'ctx> __sdk::WithDelete for GameWeatherScheduleTableHandle<'ctx> {
    type DeleteCallbackId = GameWeatherScheduleDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameWeatherScheduleDeleteCallbackId {
        GameWeatherScheduleDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameWeatherScheduleDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct GameWeatherScheduleUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for GameWeatherScheduleTableHandle<'ctx> {
    type UpdateCallbackId = GameWeatherScheduleUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameWeatherScheduleUpdateCallbackId {
        GameWeatherScheduleUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameWeatherScheduleUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

impl<'ctx> __sdk::WithUpdate for GameWeatherScheduleTableHandle<'ctx> {
    type UpdateCallbackId = GameWeatherScheduleUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameWeatherScheduleUpdateCallbackId {
        GameWeatherScheduleUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameWeatherScheduleUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `scheduled_id` unique index on the table `game_weather_schedule`,
/// which allows point queries on the field of the same name
/// via the [`GameWeatherScheduleScheduledIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_weather_schedule().scheduled_id().find(...)`.
pub struct GameWeatherScheduleScheduledIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<WeatherSchedule, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> GameWeatherScheduleTableHandle<'ctx> {
    /// Get a handle on the `scheduled_id` unique index on the table `game_weather_schedule`.
    pub fn scheduled_id(&self) -> GameWeatherScheduleScheduledIdUnique<'ctx> {
        GameWeatherScheduleScheduledIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("scheduled_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> GameWeatherScheduleScheduledIdUnique<'ctx> {
    /// Find the subscribed row whose `scheduled_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<WeatherSchedule> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<WeatherSchedule>("game_weather_schedule");
    _table.add_unique_constraint::<u64>("scheduled_id", |row| &row.scheduled_id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<WeatherSchedule>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<WeatherSchedule>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `WeatherSchedule`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait game_weather_scheduleQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `WeatherSchedule`.
    fn game_weather_schedule(&self) -> __sdk::__query_builder::Table<WeatherSchedule>;
}

impl game_weather_scheduleQueryTableAccess for __sdk::QueryTableAccessor {
    fn game_weather_schedule(&self) -> __sdk::__query_builder::Table<WeatherSchedule> {
        __sdk::__query_builder::Table::new("game_weather_schedule")
    }
}
