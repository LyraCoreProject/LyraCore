// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::creature_ai_forced_despawn_type::CreatureAiForcedDespawn;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct FireEventaiForcedDespawnArgs {
    pub row: CreatureAiForcedDespawn,
}

impl From<FireEventaiForcedDespawnArgs> for super::Reducer {
    fn from(args: FireEventaiForcedDespawnArgs) -> Self {
        Self::FireEventaiForcedDespawn { row: args.row }
    }
}

impl __sdk::InModule for FireEventaiForcedDespawnArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `fire_eventai_forced_despawn`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait fire_eventai_forced_despawn {
    /// Request that the remote module invoke the reducer `fire_eventai_forced_despawn` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`fire_eventai_forced_despawn:fire_eventai_forced_despawn_then`] to run a callback after the reducer completes.
    fn fire_eventai_forced_despawn(&self, row: CreatureAiForcedDespawn) -> __sdk::Result<()> {
        self.fire_eventai_forced_despawn_then(row, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `fire_eventai_forced_despawn` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn fire_eventai_forced_despawn_then(
        &self,
        row: CreatureAiForcedDespawn,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl fire_eventai_forced_despawn for super::RemoteReducers {
    fn fire_eventai_forced_despawn_then(
        &self,
        row: CreatureAiForcedDespawn,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(FireEventaiForcedDespawnArgs { row }, callback)
    }
}
