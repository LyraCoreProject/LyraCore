// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct PkgDeadminesResetArgs {
    pub instance_id: u64,
}

impl From<PkgDeadminesResetArgs> for super::Reducer {
    fn from(args: PkgDeadminesResetArgs) -> Self {
        Self::PkgDeadminesReset {
            instance_id: args.instance_id,
        }
    }
}

impl __sdk::InModule for PkgDeadminesResetArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `pkg_deadmines_reset`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait pkg_deadmines_reset {
    /// Request that the remote module invoke the reducer `pkg_deadmines_reset` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`pkg_deadmines_reset:pkg_deadmines_reset_then`] to run a callback after the reducer completes.
    fn pkg_deadmines_reset(&self, instance_id: u64) -> __sdk::Result<()> {
        self.pkg_deadmines_reset_then(instance_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `pkg_deadmines_reset` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn pkg_deadmines_reset_then(
        &self,
        instance_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl pkg_deadmines_reset for super::RemoteReducers {
    fn pkg_deadmines_reset_then(
        &self,
        instance_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(PkgDeadminesResetArgs { instance_id }, callback)
    }
}
