// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::graveyard_loc_type::GraveyardLoc;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `game_graveyard`.
///
/// Obtain a handle from the [`GameGraveyardTableAccess::game_graveyard`] method on [`super::RemoteTables`],
/// like `ctx.db.game_graveyard()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_graveyard().on_insert(...)`.
pub struct GameGraveyardTableHandle<'ctx> {
    imp: __sdk::TableHandle<GraveyardLoc>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `game_graveyard`.
///
/// Implemented for [`super::RemoteTables`].
pub trait GameGraveyardTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`GameGraveyardTableHandle`], which mediates access to the table `game_graveyard`.
    fn game_graveyard(&self) -> GameGraveyardTableHandle<'_>;
}

impl GameGraveyardTableAccess for super::RemoteTables {
    fn game_graveyard(&self) -> GameGraveyardTableHandle<'_> {
        GameGraveyardTableHandle {
            imp: self.imp.get_table::<GraveyardLoc>("game_graveyard"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct GameGraveyardInsertCallbackId(__sdk::CallbackId);
pub struct GameGraveyardDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for GameGraveyardTableHandle<'ctx> {
    type Row = GraveyardLoc;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = GraveyardLoc> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = GameGraveyardInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameGraveyardInsertCallbackId {
        GameGraveyardInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameGraveyardInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = GameGraveyardDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameGraveyardDeleteCallbackId {
        GameGraveyardDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameGraveyardDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct GameGraveyardUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for GameGraveyardTableHandle<'ctx> {
    type UpdateCallbackId = GameGraveyardUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameGraveyardUpdateCallbackId {
        GameGraveyardUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameGraveyardUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `game_graveyard`,
/// which allows point queries on the field of the same name
/// via the [`GameGraveyardIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_graveyard().id().find(...)`.
pub struct GameGraveyardIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<GraveyardLoc, u32>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> GameGraveyardTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `game_graveyard`.
    pub fn id(&self) -> GameGraveyardIdUnique<'ctx> {
        GameGraveyardIdUnique {
            imp: self.imp.get_unique_constraint::<u32>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> GameGraveyardIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u32) -> Option<GraveyardLoc> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<GraveyardLoc>("game_graveyard");
    _table.add_unique_constraint::<u32>("id", |row| &row.id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<GraveyardLoc>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<GraveyardLoc>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `GraveyardLoc`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait game_graveyardQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `GraveyardLoc`.
    fn game_graveyard(&self) -> __sdk::__query_builder::Table<GraveyardLoc>;
}

impl game_graveyardQueryTableAccess for __sdk::QueryTableAccessor {
    fn game_graveyard(&self) -> __sdk::__query_builder::Table<GraveyardLoc> {
        __sdk::__query_builder::Table::new("game_graveyard")
    }
}
