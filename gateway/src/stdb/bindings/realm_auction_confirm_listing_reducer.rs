// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RealmAuctionConfirmListingArgs {
    pub operation_id: u64,
    pub auction_id: u32,
}

impl From<RealmAuctionConfirmListingArgs> for super::Reducer {
    fn from(args: RealmAuctionConfirmListingArgs) -> Self {
        Self::RealmAuctionConfirmListing {
            operation_id: args.operation_id,
            auction_id: args.auction_id,
        }
    }
}

impl __sdk::InModule for RealmAuctionConfirmListingArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `realm_auction_confirm_listing`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait realm_auction_confirm_listing {
    /// Request that the remote module invoke the reducer `realm_auction_confirm_listing` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`realm_auction_confirm_listing:realm_auction_confirm_listing_then`] to run a callback after the reducer completes.
    fn realm_auction_confirm_listing(
        &self,
        operation_id: u64,
        auction_id: u32,
    ) -> __sdk::Result<()> {
        self.realm_auction_confirm_listing_then(operation_id, auction_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `realm_auction_confirm_listing` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn realm_auction_confirm_listing_then(
        &self,
        operation_id: u64,
        auction_id: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl realm_auction_confirm_listing for super::RemoteReducers {
    fn realm_auction_confirm_listing_then(
        &self,
        operation_id: u64,
        auction_id: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RealmAuctionConfirmListingArgs {
                operation_id,
                auction_id,
            },
            callback,
        )
    }
}
