// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::world_session_token_type::WorldSessionToken;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct FenceAccountArgs {
    pub token: WorldSessionToken,
    pub account_name: String,
    pub character_guid: u64,
    pub expires_micros: i64,
}

impl From<FenceAccountArgs> for super::Reducer {
    fn from(args: FenceAccountArgs) -> Self {
        Self::FenceAccount {
            token: args.token,
            account_name: args.account_name,
            character_guid: args.character_guid,
            expires_micros: args.expires_micros,
        }
    }
}

impl __sdk::InModule for FenceAccountArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `fence_account`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait fence_account {
    /// Request that the remote module invoke the reducer `fence_account` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`fence_account:fence_account_then`] to run a callback after the reducer completes.
    fn fence_account(
        &self,
        token: WorldSessionToken,
        account_name: String,
        character_guid: u64,
        expires_micros: i64,
    ) -> __sdk::Result<()> {
        self.fence_account_then(
            token,
            account_name,
            character_guid,
            expires_micros,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `fence_account` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn fence_account_then(
        &self,
        token: WorldSessionToken,
        account_name: String,
        character_guid: u64,
        expires_micros: i64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl fence_account for super::RemoteReducers {
    fn fence_account_then(
        &self,
        token: WorldSessionToken,
        account_name: String,
        character_guid: u64,
        expires_micros: i64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            FenceAccountArgs {
                token,
                account_name,
                character_guid,
                expires_micros,
            },
            callback,
        )
    }
}
