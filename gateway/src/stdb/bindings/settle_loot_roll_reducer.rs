// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SettleLootRollArgs {
    pub corpse_guid: u64,
    pub slot: u8,
    pub winner_guid: u64,
}

impl From<SettleLootRollArgs> for super::Reducer {
    fn from(args: SettleLootRollArgs) -> Self {
        Self::SettleLootRoll {
            corpse_guid: args.corpse_guid,
            slot: args.slot,
            winner_guid: args.winner_guid,
        }
    }
}

impl __sdk::InModule for SettleLootRollArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `settle_loot_roll`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait settle_loot_roll {
    /// Request that the remote module invoke the reducer `settle_loot_roll` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`settle_loot_roll:settle_loot_roll_then`] to run a callback after the reducer completes.
    fn settle_loot_roll(&self, corpse_guid: u64, slot: u8, winner_guid: u64) -> __sdk::Result<()> {
        self.settle_loot_roll_then(corpse_guid, slot, winner_guid, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `settle_loot_roll` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn settle_loot_roll_then(
        &self,
        corpse_guid: u64,
        slot: u8,
        winner_guid: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl settle_loot_roll for super::RemoteReducers {
    fn settle_loot_roll_then(
        &self,
        corpse_guid: u64,
        slot: u8,
        winner_guid: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SettleLootRollArgs {
                corpse_guid,
                slot,
                winner_guid,
            },
            callback,
        )
    }
}
