// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SetMotionTickMsArgs {
    pub tick_ms: u64,
}

impl From<SetMotionTickMsArgs> for super::Reducer {
    fn from(args: SetMotionTickMsArgs) -> Self {
        Self::SetMotionTickMs {
            tick_ms: args.tick_ms,
        }
    }
}

impl __sdk::InModule for SetMotionTickMsArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `set_motion_tick_ms`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait set_motion_tick_ms {
    /// Request that the remote module invoke the reducer `set_motion_tick_ms` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`set_motion_tick_ms:set_motion_tick_ms_then`] to run a callback after the reducer completes.
    fn set_motion_tick_ms(&self, tick_ms: u64) -> __sdk::Result<()> {
        self.set_motion_tick_ms_then(tick_ms, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `set_motion_tick_ms` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn set_motion_tick_ms_then(
        &self,
        tick_ms: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl set_motion_tick_ms for super::RemoteReducers {
    fn set_motion_tick_ms_then(
        &self,
        tick_ms: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(SetMotionTickMsArgs { tick_ms }, callback)
    }
}
