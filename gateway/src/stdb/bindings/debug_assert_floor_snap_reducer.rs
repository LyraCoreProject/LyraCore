// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DebugAssertFloorSnapArgs {
    pub map_id: u32,
    pub creature_entry: u32,
}

impl From<DebugAssertFloorSnapArgs> for super::Reducer {
    fn from(args: DebugAssertFloorSnapArgs) -> Self {
        Self::DebugAssertFloorSnap {
            map_id: args.map_id,
            creature_entry: args.creature_entry,
        }
    }
}

impl __sdk::InModule for DebugAssertFloorSnapArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `debug_assert_floor_snap`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait debug_assert_floor_snap {
    /// Request that the remote module invoke the reducer `debug_assert_floor_snap` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`debug_assert_floor_snap:debug_assert_floor_snap_then`] to run a callback after the reducer completes.
    fn debug_assert_floor_snap(&self, map_id: u32, creature_entry: u32) -> __sdk::Result<()> {
        self.debug_assert_floor_snap_then(map_id, creature_entry, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `debug_assert_floor_snap` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn debug_assert_floor_snap_then(
        &self,
        map_id: u32,
        creature_entry: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl debug_assert_floor_snap for super::RemoteReducers {
    fn debug_assert_floor_snap_then(
        &self,
        map_id: u32,
        creature_entry: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DebugAssertFloorSnapArgs {
                map_id,
                creature_entry,
            },
            callback,
        )
    }
}
