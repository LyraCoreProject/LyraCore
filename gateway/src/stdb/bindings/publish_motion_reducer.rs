// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::motion_publish_schedule_type::MotionPublishSchedule;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct PublishMotionArgs {
    pub schedule: MotionPublishSchedule,
}

impl From<PublishMotionArgs> for super::Reducer {
    fn from(args: PublishMotionArgs) -> Self {
        Self::PublishMotion {
            schedule: args.schedule,
        }
    }
}

impl __sdk::InModule for PublishMotionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `publish_motion`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait publish_motion {
    /// Request that the remote module invoke the reducer `publish_motion` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`publish_motion:publish_motion_then`] to run a callback after the reducer completes.
    fn publish_motion(&self, schedule: MotionPublishSchedule) -> __sdk::Result<()> {
        self.publish_motion_then(schedule, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `publish_motion` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn publish_motion_then(
        &self,
        schedule: MotionPublishSchedule,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl publish_motion for super::RemoteReducers {
    fn publish_motion_then(
        &self,
        schedule: MotionPublishSchedule,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(PublishMotionArgs { schedule }, callback)
    }
}
