// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DebugStageRangedLethalDamageFloorFixtureArgs {
    pub attacker_guid: u64,
    pub target_guid: u64,
    pub damage: u32,
    pub delay_ms: u32,
}

impl From<DebugStageRangedLethalDamageFloorFixtureArgs> for super::Reducer {
    fn from(args: DebugStageRangedLethalDamageFloorFixtureArgs) -> Self {
        Self::DebugStageRangedLethalDamageFloorFixture {
            attacker_guid: args.attacker_guid,
            target_guid: args.target_guid,
            damage: args.damage,
            delay_ms: args.delay_ms,
        }
    }
}

impl __sdk::InModule for DebugStageRangedLethalDamageFloorFixtureArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `debug_stage_ranged_lethal_damage_floor_fixture`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait debug_stage_ranged_lethal_damage_floor_fixture {
    /// Request that the remote module invoke the reducer `debug_stage_ranged_lethal_damage_floor_fixture` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`debug_stage_ranged_lethal_damage_floor_fixture:debug_stage_ranged_lethal_damage_floor_fixture_then`] to run a callback after the reducer completes.
    fn debug_stage_ranged_lethal_damage_floor_fixture(
        &self,
        attacker_guid: u64,
        target_guid: u64,
        damage: u32,
        delay_ms: u32,
    ) -> __sdk::Result<()> {
        self.debug_stage_ranged_lethal_damage_floor_fixture_then(
            attacker_guid,
            target_guid,
            damage,
            delay_ms,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `debug_stage_ranged_lethal_damage_floor_fixture` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn debug_stage_ranged_lethal_damage_floor_fixture_then(
        &self,
        attacker_guid: u64,
        target_guid: u64,
        damage: u32,
        delay_ms: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl debug_stage_ranged_lethal_damage_floor_fixture for super::RemoteReducers {
    fn debug_stage_ranged_lethal_damage_floor_fixture_then(
        &self,
        attacker_guid: u64,
        target_guid: u64,
        damage: u32,
        delay_ms: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DebugStageRangedLethalDamageFloorFixtureArgs {
                attacker_guid,
                target_guid,
                damage,
                delay_ms,
            },
            callback,
        )
    }
}
