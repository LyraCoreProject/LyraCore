// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct BeginTransferArgs {
    pub transfer_id: u64,
    pub character_guid: u64,
    pub dest_map_id: u32,
    pub dest_instance_id: u64,
    pub dest_x: f32,
    pub dest_y: f32,
    pub dest_z: f32,
    pub dest_o: f32,
    pub cross_database: bool,
}

impl From<BeginTransferArgs> for super::Reducer {
    fn from(args: BeginTransferArgs) -> Self {
        Self::BeginTransfer {
            transfer_id: args.transfer_id,
            character_guid: args.character_guid,
            dest_map_id: args.dest_map_id,
            dest_instance_id: args.dest_instance_id,
            dest_x: args.dest_x,
            dest_y: args.dest_y,
            dest_z: args.dest_z,
            dest_o: args.dest_o,
            cross_database: args.cross_database,
        }
    }
}

impl __sdk::InModule for BeginTransferArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `begin_transfer`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait begin_transfer {
    /// Request that the remote module invoke the reducer `begin_transfer` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`begin_transfer:begin_transfer_then`] to run a callback after the reducer completes.
    fn begin_transfer(
        &self,
        transfer_id: u64,
        character_guid: u64,
        dest_map_id: u32,
        dest_instance_id: u64,
        dest_x: f32,
        dest_y: f32,
        dest_z: f32,
        dest_o: f32,
        cross_database: bool,
    ) -> __sdk::Result<()> {
        self.begin_transfer_then(
            transfer_id,
            character_guid,
            dest_map_id,
            dest_instance_id,
            dest_x,
            dest_y,
            dest_z,
            dest_o,
            cross_database,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `begin_transfer` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn begin_transfer_then(
        &self,
        transfer_id: u64,
        character_guid: u64,
        dest_map_id: u32,
        dest_instance_id: u64,
        dest_x: f32,
        dest_y: f32,
        dest_z: f32,
        dest_o: f32,
        cross_database: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl begin_transfer for super::RemoteReducers {
    fn begin_transfer_then(
        &self,
        transfer_id: u64,
        character_guid: u64,
        dest_map_id: u32,
        dest_instance_id: u64,
        dest_x: f32,
        dest_y: f32,
        dest_z: f32,
        dest_o: f32,
        cross_database: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            BeginTransferArgs {
                transfer_id,
                character_guid,
                dest_map_id,
                dest_instance_id,
                dest_x,
                dest_y,
                dest_z,
                dest_o,
                cross_database,
            },
            callback,
        )
    }
}
