// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ActivateVmapGenerationArgs {
    pub generation_id: u64,
}

impl From<ActivateVmapGenerationArgs> for super::Reducer {
    fn from(args: ActivateVmapGenerationArgs) -> Self {
        Self::ActivateVmapGeneration {
            generation_id: args.generation_id,
        }
    }
}

impl __sdk::InModule for ActivateVmapGenerationArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `activate_vmap_generation`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait activate_vmap_generation {
    /// Request that the remote module invoke the reducer `activate_vmap_generation` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`activate_vmap_generation:activate_vmap_generation_then`] to run a callback after the reducer completes.
    fn activate_vmap_generation(&self, generation_id: u64) -> __sdk::Result<()> {
        self.activate_vmap_generation_then(generation_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `activate_vmap_generation` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn activate_vmap_generation_then(
        &self,
        generation_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl activate_vmap_generation for super::RemoteReducers {
    fn activate_vmap_generation_then(
        &self,
        generation_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(ActivateVmapGenerationArgs { generation_id }, callback)
    }
}
