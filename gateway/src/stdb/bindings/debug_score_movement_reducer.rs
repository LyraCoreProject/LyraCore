// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DebugScoreMovementArgs {
    pub guid: u64,
    pub old_x: f32,
    pub old_y: f32,
    pub x: f32,
    pub y: f32,
    pub z: f32,
    pub old_move_ms: u32,
    pub move_time_ms: u32,
}

impl From<DebugScoreMovementArgs> for super::Reducer {
    fn from(args: DebugScoreMovementArgs) -> Self {
        Self::DebugScoreMovement {
            guid: args.guid,
            old_x: args.old_x,
            old_y: args.old_y,
            x: args.x,
            y: args.y,
            z: args.z,
            old_move_ms: args.old_move_ms,
            move_time_ms: args.move_time_ms,
        }
    }
}

impl __sdk::InModule for DebugScoreMovementArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `debug_score_movement`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait debug_score_movement {
    /// Request that the remote module invoke the reducer `debug_score_movement` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`debug_score_movement:debug_score_movement_then`] to run a callback after the reducer completes.
    fn debug_score_movement(
        &self,
        guid: u64,
        old_x: f32,
        old_y: f32,
        x: f32,
        y: f32,
        z: f32,
        old_move_ms: u32,
        move_time_ms: u32,
    ) -> __sdk::Result<()> {
        self.debug_score_movement_then(
            guid,
            old_x,
            old_y,
            x,
            y,
            z,
            old_move_ms,
            move_time_ms,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `debug_score_movement` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn debug_score_movement_then(
        &self,
        guid: u64,
        old_x: f32,
        old_y: f32,
        x: f32,
        y: f32,
        z: f32,
        old_move_ms: u32,
        move_time_ms: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl debug_score_movement for super::RemoteReducers {
    fn debug_score_movement_then(
        &self,
        guid: u64,
        old_x: f32,
        old_y: f32,
        x: f32,
        y: f32,
        z: f32,
        old_move_ms: u32,
        move_time_ms: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DebugScoreMovementArgs {
                guid,
                old_x,
                old_y,
                x,
                y,
                z,
                old_move_ms,
                move_time_ms,
            },
            callback,
        )
    }
}
