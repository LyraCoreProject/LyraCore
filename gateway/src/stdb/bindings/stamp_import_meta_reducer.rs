// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct StampImportMetaArgs {
    pub family: String,
    pub source_sha: String,
    pub file_hash: String,
    pub row_count: u64,
}

impl From<StampImportMetaArgs> for super::Reducer {
    fn from(args: StampImportMetaArgs) -> Self {
        Self::StampImportMeta {
            family: args.family,
            source_sha: args.source_sha,
            file_hash: args.file_hash,
            row_count: args.row_count,
        }
    }
}

impl __sdk::InModule for StampImportMetaArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `stamp_import_meta`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait stamp_import_meta {
    /// Request that the remote module invoke the reducer `stamp_import_meta` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`stamp_import_meta:stamp_import_meta_then`] to run a callback after the reducer completes.
    fn stamp_import_meta(
        &self,
        family: String,
        source_sha: String,
        file_hash: String,
        row_count: u64,
    ) -> __sdk::Result<()> {
        self.stamp_import_meta_then(family, source_sha, file_hash, row_count, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `stamp_import_meta` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn stamp_import_meta_then(
        &self,
        family: String,
        source_sha: String,
        file_hash: String,
        row_count: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl stamp_import_meta for super::RemoteReducers {
    fn stamp_import_meta_then(
        &self,
        family: String,
        source_sha: String,
        file_hash: String,
        row_count: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            StampImportMetaArgs {
                family,
                source_sha,
                file_hash,
                row_count,
            },
            callback,
        )
    }
}
