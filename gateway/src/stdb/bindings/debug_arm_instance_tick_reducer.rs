// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DebugArmInstanceTickArgs {
    pub instance_id: u64,
    pub tick_ms: u64,
}

impl From<DebugArmInstanceTickArgs> for super::Reducer {
    fn from(args: DebugArmInstanceTickArgs) -> Self {
        Self::DebugArmInstanceTick {
            instance_id: args.instance_id,
            tick_ms: args.tick_ms,
        }
    }
}

impl __sdk::InModule for DebugArmInstanceTickArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `debug_arm_instance_tick`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait debug_arm_instance_tick {
    /// Request that the remote module invoke the reducer `debug_arm_instance_tick` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`debug_arm_instance_tick:debug_arm_instance_tick_then`] to run a callback after the reducer completes.
    fn debug_arm_instance_tick(&self, instance_id: u64, tick_ms: u64) -> __sdk::Result<()> {
        self.debug_arm_instance_tick_then(instance_id, tick_ms, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `debug_arm_instance_tick` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn debug_arm_instance_tick_then(
        &self,
        instance_id: u64,
        tick_ms: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl debug_arm_instance_tick for super::RemoteReducers {
    fn debug_arm_instance_tick_then(
        &self,
        instance_id: u64,
        tick_ms: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DebugArmInstanceTickArgs {
                instance_id,
                tick_ms,
            },
            callback,
        )
    }
}
