// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SetCharacterShardArgs {
    pub character_guid: u64,
    pub map_id: u32,
    pub instance_id: u64,
}

impl From<SetCharacterShardArgs> for super::Reducer {
    fn from(args: SetCharacterShardArgs) -> Self {
        Self::SetCharacterShard {
            character_guid: args.character_guid,
            map_id: args.map_id,
            instance_id: args.instance_id,
        }
    }
}

impl __sdk::InModule for SetCharacterShardArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `set_character_shard`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait set_character_shard {
    /// Request that the remote module invoke the reducer `set_character_shard` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`set_character_shard:set_character_shard_then`] to run a callback after the reducer completes.
    fn set_character_shard(
        &self,
        character_guid: u64,
        map_id: u32,
        instance_id: u64,
    ) -> __sdk::Result<()> {
        self.set_character_shard_then(character_guid, map_id, instance_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `set_character_shard` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn set_character_shard_then(
        &self,
        character_guid: u64,
        map_id: u32,
        instance_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl set_character_shard for super::RemoteReducers {
    fn set_character_shard_then(
        &self,
        character_guid: u64,
        map_id: u32,
        instance_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SetCharacterShardArgs {
                character_guid,
                map_id,
                instance_id,
            },
            callback,
        )
    }
}
