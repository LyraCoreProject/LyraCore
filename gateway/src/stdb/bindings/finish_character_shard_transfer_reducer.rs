// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::session_actor_type::SessionActor;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct FinishCharacterShardTransferArgs {
    pub character_guid: u64,
    pub source_map_id: u32,
    pub source_instance_id: u64,
    pub source_revision: u64,
    pub destination_map_id: u32,
    pub destination_instance_id: u64,
    pub source_module_identity: __sdk::Identity,
    pub transfer_intent_id: u64,
    pub controller_generation: u64,
    pub request_actor: SessionActor,
}

impl From<FinishCharacterShardTransferArgs> for super::Reducer {
    fn from(args: FinishCharacterShardTransferArgs) -> Self {
        Self::FinishCharacterShardTransfer {
            character_guid: args.character_guid,
            source_map_id: args.source_map_id,
            source_instance_id: args.source_instance_id,
            source_revision: args.source_revision,
            destination_map_id: args.destination_map_id,
            destination_instance_id: args.destination_instance_id,
            source_module_identity: args.source_module_identity,
            transfer_intent_id: args.transfer_intent_id,
            controller_generation: args.controller_generation,
            request_actor: args.request_actor,
        }
    }
}

impl __sdk::InModule for FinishCharacterShardTransferArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `finish_character_shard_transfer`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait finish_character_shard_transfer {
    /// Request that the remote module invoke the reducer `finish_character_shard_transfer` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`finish_character_shard_transfer:finish_character_shard_transfer_then`] to run a callback after the reducer completes.
    fn finish_character_shard_transfer(
        &self,
        character_guid: u64,
        source_map_id: u32,
        source_instance_id: u64,
        source_revision: u64,
        destination_map_id: u32,
        destination_instance_id: u64,
        source_module_identity: __sdk::Identity,
        transfer_intent_id: u64,
        controller_generation: u64,
        request_actor: SessionActor,
    ) -> __sdk::Result<()> {
        self.finish_character_shard_transfer_then(
            character_guid,
            source_map_id,
            source_instance_id,
            source_revision,
            destination_map_id,
            destination_instance_id,
            source_module_identity,
            transfer_intent_id,
            controller_generation,
            request_actor,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `finish_character_shard_transfer` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn finish_character_shard_transfer_then(
        &self,
        character_guid: u64,
        source_map_id: u32,
        source_instance_id: u64,
        source_revision: u64,
        destination_map_id: u32,
        destination_instance_id: u64,
        source_module_identity: __sdk::Identity,
        transfer_intent_id: u64,
        controller_generation: u64,
        request_actor: SessionActor,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl finish_character_shard_transfer for super::RemoteReducers {
    fn finish_character_shard_transfer_then(
        &self,
        character_guid: u64,
        source_map_id: u32,
        source_instance_id: u64,
        source_revision: u64,
        destination_map_id: u32,
        destination_instance_id: u64,
        source_module_identity: __sdk::Identity,
        transfer_intent_id: u64,
        controller_generation: u64,
        request_actor: SessionActor,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            FinishCharacterShardTransferArgs {
                character_guid,
                source_map_id,
                source_instance_id,
                source_revision,
                destination_map_id,
                destination_instance_id,
                source_module_identity,
                transfer_intent_id,
                controller_generation,
                request_actor,
            },
            callback,
        )
    }
}
