// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::duel_schedule_type::DuelSchedule;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct TickDuelsArgs {
    pub schedule: DuelSchedule,
}

impl From<TickDuelsArgs> for super::Reducer {
    fn from(args: TickDuelsArgs) -> Self {
        Self::TickDuels {
            schedule: args.schedule,
        }
    }
}

impl __sdk::InModule for TickDuelsArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `tick_duels`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait tick_duels {
    /// Request that the remote module invoke the reducer `tick_duels` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`tick_duels:tick_duels_then`] to run a callback after the reducer completes.
    fn tick_duels(&self, schedule: DuelSchedule) -> __sdk::Result<()> {
        self.tick_duels_then(schedule, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `tick_duels` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn tick_duels_then(
        &self,
        schedule: DuelSchedule,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl tick_duels for super::RemoteReducers {
    fn tick_duels_then(
        &self,
        schedule: DuelSchedule,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(TickDuelsArgs { schedule }, callback)
    }
}
