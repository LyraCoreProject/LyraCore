// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ClaimAccountArgs {
    pub account_id: u64,
    pub character_guid: u64,
    pub request_nonce: u128,
}

impl From<ClaimAccountArgs> for super::Reducer {
    fn from(args: ClaimAccountArgs) -> Self {
        Self::ClaimAccount {
            account_id: args.account_id,
            character_guid: args.character_guid,
            request_nonce: args.request_nonce,
        }
    }
}

impl __sdk::InModule for ClaimAccountArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `claim_account`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait claim_account {
    /// Request that the remote module invoke the reducer `claim_account` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`claim_account:claim_account_then`] to run a callback after the reducer completes.
    fn claim_account(
        &self,
        account_id: u64,
        character_guid: u64,
        request_nonce: u128,
    ) -> __sdk::Result<()> {
        self.claim_account_then(account_id, character_guid, request_nonce, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `claim_account` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn claim_account_then(
        &self,
        account_id: u64,
        character_guid: u64,
        request_nonce: u128,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl claim_account for super::RemoteReducers {
    fn claim_account_then(
        &self,
        account_id: u64,
        character_guid: u64,
        request_nonce: u128,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ClaimAccountArgs {
                account_id,
                character_guid,
                request_nonce,
            },
            callback,
        )
    }
}
