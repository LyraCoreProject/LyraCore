// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::guild_op_type::GuildOp;
use super::session_actor_type::SessionActor;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RealmGuildOpArgs {
    pub request_actor: SessionActor,
    pub op: GuildOp,
}

impl From<RealmGuildOpArgs> for super::Reducer {
    fn from(args: RealmGuildOpArgs) -> Self {
        Self::RealmGuildOp {
            request_actor: args.request_actor,
            op: args.op,
        }
    }
}

impl __sdk::InModule for RealmGuildOpArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `realm_guild_op`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait realm_guild_op {
    /// Request that the remote module invoke the reducer `realm_guild_op` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`realm_guild_op:realm_guild_op_then`] to run a callback after the reducer completes.
    fn realm_guild_op(&self, request_actor: SessionActor, op: GuildOp) -> __sdk::Result<()> {
        self.realm_guild_op_then(request_actor, op, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `realm_guild_op` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn realm_guild_op_then(
        &self,
        request_actor: SessionActor,
        op: GuildOp,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl realm_guild_op for super::RemoteReducers {
    fn realm_guild_op_then(
        &self,
        request_actor: SessionActor,
        op: GuildOp,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(RealmGuildOpArgs { request_actor, op }, callback)
    }
}
