// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::pending_spell_impact_type::PendingSpellImpact;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct FireSpellImpactArgs {
    pub sched: PendingSpellImpact,
}

impl From<FireSpellImpactArgs> for super::Reducer {
    fn from(args: FireSpellImpactArgs) -> Self {
        Self::FireSpellImpact { sched: args.sched }
    }
}

impl __sdk::InModule for FireSpellImpactArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `fire_spell_impact`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait fire_spell_impact {
    /// Request that the remote module invoke the reducer `fire_spell_impact` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`fire_spell_impact:fire_spell_impact_then`] to run a callback after the reducer completes.
    fn fire_spell_impact(&self, sched: PendingSpellImpact) -> __sdk::Result<()> {
        self.fire_spell_impact_then(sched, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `fire_spell_impact` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn fire_spell_impact_then(
        &self,
        sched: PendingSpellImpact,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl fire_spell_impact for super::RemoteReducers {
    fn fire_spell_impact_then(
        &self,
        sched: PendingSpellImpact,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(FireSpellImpactArgs { sched }, callback)
    }
}
