// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::skill_availability_type::SkillAvailability;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `game_skill_availability`.
///
/// Obtain a handle from the [`GameSkillAvailabilityTableAccess::game_skill_availability`] method on [`super::RemoteTables`],
/// like `ctx.db.game_skill_availability()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_skill_availability().on_insert(...)`.
pub struct GameSkillAvailabilityTableHandle<'ctx> {
    imp: __sdk::TableHandle<SkillAvailability>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `game_skill_availability`.
///
/// Implemented for [`super::RemoteTables`].
pub trait GameSkillAvailabilityTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`GameSkillAvailabilityTableHandle`], which mediates access to the table `game_skill_availability`.
    fn game_skill_availability(&self) -> GameSkillAvailabilityTableHandle<'_>;
}

impl GameSkillAvailabilityTableAccess for super::RemoteTables {
    fn game_skill_availability(&self) -> GameSkillAvailabilityTableHandle<'_> {
        GameSkillAvailabilityTableHandle {
            imp: self
                .imp
                .get_table::<SkillAvailability>("game_skill_availability"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct GameSkillAvailabilityInsertCallbackId(__sdk::CallbackId);
pub struct GameSkillAvailabilityDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for GameSkillAvailabilityTableHandle<'ctx> {
    type Row = SkillAvailability;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = SkillAvailability> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = GameSkillAvailabilityInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameSkillAvailabilityInsertCallbackId {
        GameSkillAvailabilityInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameSkillAvailabilityInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = GameSkillAvailabilityDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameSkillAvailabilityDeleteCallbackId {
        GameSkillAvailabilityDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameSkillAvailabilityDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct GameSkillAvailabilityUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for GameSkillAvailabilityTableHandle<'ctx> {
    type UpdateCallbackId = GameSkillAvailabilityUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameSkillAvailabilityUpdateCallbackId {
        GameSkillAvailabilityUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameSkillAvailabilityUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `game_skill_availability`,
/// which allows point queries on the field of the same name
/// via the [`GameSkillAvailabilityIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_skill_availability().id().find(...)`.
pub struct GameSkillAvailabilityIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<SkillAvailability, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> GameSkillAvailabilityTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `game_skill_availability`.
    pub fn id(&self) -> GameSkillAvailabilityIdUnique<'ctx> {
        GameSkillAvailabilityIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> GameSkillAvailabilityIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<SkillAvailability> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<SkillAvailability>("game_skill_availability");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<SkillAvailability>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<SkillAvailability>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `SkillAvailability`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait game_skill_availabilityQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `SkillAvailability`.
    fn game_skill_availability(&self) -> __sdk::__query_builder::Table<SkillAvailability>;
}

impl game_skill_availabilityQueryTableAccess for __sdk::QueryTableAccessor {
    fn game_skill_availability(&self) -> __sdk::__query_builder::Table<SkillAvailability> {
        __sdk::__query_builder::Table::new("game_skill_availability")
    }
}
