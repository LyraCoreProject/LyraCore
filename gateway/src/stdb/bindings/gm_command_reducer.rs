// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct GmCommandArgs {
    pub text: String,
}

impl From<GmCommandArgs> for super::Reducer {
    fn from(args: GmCommandArgs) -> Self {
        Self::GmCommand { text: args.text }
    }
}

impl __sdk::InModule for GmCommandArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `gm_command`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait gm_command {
    /// Request that the remote module invoke the reducer `gm_command` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`gm_command:gm_command_then`] to run a callback after the reducer completes.
    fn gm_command(&self, text: String) -> __sdk::Result<()> {
        self.gm_command_then(text, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `gm_command` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn gm_command_then(
        &self,
        text: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl gm_command for super::RemoteReducers {
    fn gm_command_then(
        &self,
        text: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(GmCommandArgs { text }, callback)
    }
}
