// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct GwAuctionListLocalArgs {
    pub operation_id: u64,
    pub seller_guid: u64,
    pub item_guid: u64,
    pub auctioneer_guid: u64,
    pub house: u32,
    pub start_bid: u32,
    pub buyout: u32,
    pub duration_minutes: u32,
}

impl From<GwAuctionListLocalArgs> for super::Reducer {
    fn from(args: GwAuctionListLocalArgs) -> Self {
        Self::GwAuctionListLocal {
            operation_id: args.operation_id,
            seller_guid: args.seller_guid,
            item_guid: args.item_guid,
            auctioneer_guid: args.auctioneer_guid,
            house: args.house,
            start_bid: args.start_bid,
            buyout: args.buyout,
            duration_minutes: args.duration_minutes,
        }
    }
}

impl __sdk::InModule for GwAuctionListLocalArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `gw_auction_list_local`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait gw_auction_list_local {
    /// Request that the remote module invoke the reducer `gw_auction_list_local` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`gw_auction_list_local:gw_auction_list_local_then`] to run a callback after the reducer completes.
    fn gw_auction_list_local(
        &self,
        operation_id: u64,
        seller_guid: u64,
        item_guid: u64,
        auctioneer_guid: u64,
        house: u32,
        start_bid: u32,
        buyout: u32,
        duration_minutes: u32,
    ) -> __sdk::Result<()> {
        self.gw_auction_list_local_then(
            operation_id,
            seller_guid,
            item_guid,
            auctioneer_guid,
            house,
            start_bid,
            buyout,
            duration_minutes,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `gw_auction_list_local` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn gw_auction_list_local_then(
        &self,
        operation_id: u64,
        seller_guid: u64,
        item_guid: u64,
        auctioneer_guid: u64,
        house: u32,
        start_bid: u32,
        buyout: u32,
        duration_minutes: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl gw_auction_list_local for super::RemoteReducers {
    fn gw_auction_list_local_then(
        &self,
        operation_id: u64,
        seller_guid: u64,
        item_guid: u64,
        auctioneer_guid: u64,
        house: u32,
        start_bid: u32,
        buyout: u32,
        duration_minutes: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            GwAuctionListLocalArgs {
                operation_id,
                seller_guid,
                item_guid,
                auctioneer_guid,
                house,
                start_bid,
                buyout,
                duration_minutes,
            },
            callback,
        )
    }
}
