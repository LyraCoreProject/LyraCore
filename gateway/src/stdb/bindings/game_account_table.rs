// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::account_type::Account;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `game_account`.
///
/// Obtain a handle from the [`GameAccountTableAccess::game_account`] method on [`super::RemoteTables`],
/// like `ctx.db.game_account()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_account().on_insert(...)`.
pub struct GameAccountTableHandle<'ctx> {
    imp: __sdk::TableHandle<Account>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `game_account`.
///
/// Implemented for [`super::RemoteTables`].
pub trait GameAccountTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`GameAccountTableHandle`], which mediates access to the table `game_account`.
    fn game_account(&self) -> GameAccountTableHandle<'_>;
}

impl GameAccountTableAccess for super::RemoteTables {
    fn game_account(&self) -> GameAccountTableHandle<'_> {
        GameAccountTableHandle {
            imp: self.imp.get_table::<Account>("game_account"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct GameAccountInsertCallbackId(__sdk::CallbackId);
pub struct GameAccountDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for GameAccountTableHandle<'ctx> {
    type Row = Account;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = Account> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = GameAccountInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameAccountInsertCallbackId {
        GameAccountInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameAccountInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = GameAccountDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameAccountDeleteCallbackId {
        GameAccountDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameAccountDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct GameAccountUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for GameAccountTableHandle<'ctx> {
    type UpdateCallbackId = GameAccountUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameAccountUpdateCallbackId {
        GameAccountUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameAccountUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `game_account`,
/// which allows point queries on the field of the same name
/// via the [`GameAccountIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_account().id().find(...)`.
pub struct GameAccountIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<Account, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> GameAccountTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `game_account`.
    pub fn id(&self) -> GameAccountIdUnique<'ctx> {
        GameAccountIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> GameAccountIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<Account> {
        self.imp.find(col_val)
    }
}

/// Access to the `username` unique index on the table `game_account`,
/// which allows point queries on the field of the same name
/// via the [`GameAccountUsernameUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_account().username().find(...)`.
pub struct GameAccountUsernameUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<Account, String>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> GameAccountTableHandle<'ctx> {
    /// Get a handle on the `username` unique index on the table `game_account`.
    pub fn username(&self) -> GameAccountUsernameUnique<'ctx> {
        GameAccountUsernameUnique {
            imp: self.imp.get_unique_constraint::<String>("username"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> GameAccountUsernameUnique<'ctx> {
    /// Find the subscribed row whose `username` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &String) -> Option<Account> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<Account>("game_account");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
    _table.add_unique_constraint::<String>("username", |row| &row.username);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<Account>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<Account>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `Account`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait game_accountQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `Account`.
    fn game_account(&self) -> __sdk::__query_builder::Table<Account>;
}

impl game_accountQueryTableAccess for __sdk::QueryTableAccessor {
    fn game_account(&self) -> __sdk::__query_builder::Table<Account> {
        __sdk::__query_builder::Table::new("game_account")
    }
}
