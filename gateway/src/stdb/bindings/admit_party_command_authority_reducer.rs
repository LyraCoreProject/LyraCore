// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AdmitPartyCommandAuthorityArgs {
    pub group_id: u64,
    pub leader_guid: u64,
    pub bot_guid: u64,
    pub authority_member_guid: u64,
    pub expected_members: Vec<u64>,
}

impl From<AdmitPartyCommandAuthorityArgs> for super::Reducer {
    fn from(args: AdmitPartyCommandAuthorityArgs) -> Self {
        Self::AdmitPartyCommandAuthority {
            group_id: args.group_id,
            leader_guid: args.leader_guid,
            bot_guid: args.bot_guid,
            authority_member_guid: args.authority_member_guid,
            expected_members: args.expected_members,
        }
    }
}

impl __sdk::InModule for AdmitPartyCommandAuthorityArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `admit_party_command_authority`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait admit_party_command_authority {
    /// Request that the remote module invoke the reducer `admit_party_command_authority` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`admit_party_command_authority:admit_party_command_authority_then`] to run a callback after the reducer completes.
    fn admit_party_command_authority(
        &self,
        group_id: u64,
        leader_guid: u64,
        bot_guid: u64,
        authority_member_guid: u64,
        expected_members: Vec<u64>,
    ) -> __sdk::Result<()> {
        self.admit_party_command_authority_then(
            group_id,
            leader_guid,
            bot_guid,
            authority_member_guid,
            expected_members,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `admit_party_command_authority` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn admit_party_command_authority_then(
        &self,
        group_id: u64,
        leader_guid: u64,
        bot_guid: u64,
        authority_member_guid: u64,
        expected_members: Vec<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl admit_party_command_authority for super::RemoteReducers {
    fn admit_party_command_authority_then(
        &self,
        group_id: u64,
        leader_guid: u64,
        bot_guid: u64,
        authority_member_guid: u64,
        expected_members: Vec<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AdmitPartyCommandAuthorityArgs {
                group_id,
                leader_guid,
                bot_guid,
                authority_member_guid,
                expected_members,
            },
            callback,
        )
    }
}
