// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DebugEncounterResetArgs {
    pub instance_id: u64,
    pub encounter_id: u32,
}

impl From<DebugEncounterResetArgs> for super::Reducer {
    fn from(args: DebugEncounterResetArgs) -> Self {
        Self::DebugEncounterReset {
            instance_id: args.instance_id,
            encounter_id: args.encounter_id,
        }
    }
}

impl __sdk::InModule for DebugEncounterResetArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `debug_encounter_reset`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait debug_encounter_reset {
    /// Request that the remote module invoke the reducer `debug_encounter_reset` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`debug_encounter_reset:debug_encounter_reset_then`] to run a callback after the reducer completes.
    fn debug_encounter_reset(&self, instance_id: u64, encounter_id: u32) -> __sdk::Result<()> {
        self.debug_encounter_reset_then(instance_id, encounter_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `debug_encounter_reset` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn debug_encounter_reset_then(
        &self,
        instance_id: u64,
        encounter_id: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl debug_encounter_reset for super::RemoteReducers {
    fn debug_encounter_reset_then(
        &self,
        instance_id: u64,
        encounter_id: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DebugEncounterResetArgs {
                instance_id,
                encounter_id,
            },
            callback,
        )
    }
}
