// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SetTargetArgs {
    pub target_guid: u64,
}

impl From<SetTargetArgs> for super::Reducer {
    fn from(args: SetTargetArgs) -> Self {
        Self::SetTarget {
            target_guid: args.target_guid,
        }
    }
}

impl __sdk::InModule for SetTargetArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `set_target`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait set_target {
    /// Request that the remote module invoke the reducer `set_target` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`set_target:set_target_then`] to run a callback after the reducer completes.
    fn set_target(&self, target_guid: u64) -> __sdk::Result<()> {
        self.set_target_then(target_guid, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `set_target` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn set_target_then(
        &self,
        target_guid: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl set_target for super::RemoteReducers {
    fn set_target_then(
        &self,
        target_guid: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(SetTargetArgs { target_guid }, callback)
    }
}
