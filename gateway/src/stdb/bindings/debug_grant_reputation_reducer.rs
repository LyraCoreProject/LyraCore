// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DebugGrantReputationArgs {
    pub character_guid: u64,
    pub faction_id: u32,
    pub amount: i32,
}

impl From<DebugGrantReputationArgs> for super::Reducer {
    fn from(args: DebugGrantReputationArgs) -> Self {
        Self::DebugGrantReputation {
            character_guid: args.character_guid,
            faction_id: args.faction_id,
            amount: args.amount,
        }
    }
}

impl __sdk::InModule for DebugGrantReputationArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `debug_grant_reputation`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait debug_grant_reputation {
    /// Request that the remote module invoke the reducer `debug_grant_reputation` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`debug_grant_reputation:debug_grant_reputation_then`] to run a callback after the reducer completes.
    fn debug_grant_reputation(
        &self,
        character_guid: u64,
        faction_id: u32,
        amount: i32,
    ) -> __sdk::Result<()> {
        self.debug_grant_reputation_then(character_guid, faction_id, amount, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `debug_grant_reputation` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn debug_grant_reputation_then(
        &self,
        character_guid: u64,
        faction_id: u32,
        amount: i32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl debug_grant_reputation for super::RemoteReducers {
    fn debug_grant_reputation_then(
        &self,
        character_guid: u64,
        faction_id: u32,
        amount: i32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DebugGrantReputationArgs {
                character_guid,
                faction_id,
                amount,
            },
            callback,
        )
    }
}
