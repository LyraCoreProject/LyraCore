// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::creature_move_schedule_type::CreatureMoveSchedule;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct TickCreaturesArgs {
    pub schedule: CreatureMoveSchedule,
}

impl From<TickCreaturesArgs> for super::Reducer {
    fn from(args: TickCreaturesArgs) -> Self {
        Self::TickCreatures {
            schedule: args.schedule,
        }
    }
}

impl __sdk::InModule for TickCreaturesArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `tick_creatures`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait tick_creatures {
    /// Request that the remote module invoke the reducer `tick_creatures` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`tick_creatures:tick_creatures_then`] to run a callback after the reducer completes.
    fn tick_creatures(&self, schedule: CreatureMoveSchedule) -> __sdk::Result<()> {
        self.tick_creatures_then(schedule, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `tick_creatures` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn tick_creatures_then(
        &self,
        schedule: CreatureMoveSchedule,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl tick_creatures for super::RemoteReducers {
    fn tick_creatures_then(
        &self,
        schedule: CreatureMoveSchedule,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(TickCreaturesArgs { schedule }, callback)
    }
}
