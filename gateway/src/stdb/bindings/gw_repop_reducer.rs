// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::session_actor_type::SessionActor;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct GwRepopArgs {
    pub request_actor: SessionActor,
}

impl From<GwRepopArgs> for super::Reducer {
    fn from(args: GwRepopArgs) -> Self {
        Self::GwRepop {
            request_actor: args.request_actor,
        }
    }
}

impl __sdk::InModule for GwRepopArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `gw_repop`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait gw_repop {
    /// Request that the remote module invoke the reducer `gw_repop` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`gw_repop:gw_repop_then`] to run a callback after the reducer completes.
    fn gw_repop(&self, request_actor: SessionActor) -> __sdk::Result<()> {
        self.gw_repop_then(request_actor, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `gw_repop` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn gw_repop_then(
        &self,
        request_actor: SessionActor,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl gw_repop for super::RemoteReducers {
    fn gw_repop_then(
        &self,
        request_actor: SessionActor,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(GwRepopArgs { request_actor }, callback)
    }
}
