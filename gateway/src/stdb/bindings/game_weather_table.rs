// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::zone_weather_chance_type::ZoneWeatherChance;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `game_weather`.
///
/// Obtain a handle from the [`GameWeatherTableAccess::game_weather`] method on [`super::RemoteTables`],
/// like `ctx.db.game_weather()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_weather().on_insert(...)`.
pub struct GameWeatherTableHandle<'ctx> {
    imp: __sdk::TableHandle<ZoneWeatherChance>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

/// Lifetime-aware accessor marker for the table `game_weather`.
pub struct GameWeatherTableAccessor;

impl __sdk::TableAccessor<super::RemoteTables> for GameWeatherTableAccessor {
    type Row = ZoneWeatherChance;
    type Handle<'db> = GameWeatherTableHandle<'db>;

    fn get<'db>(db: &'db super::RemoteTables) -> Self::Handle<'db> {
        db.game_weather()
    }
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `game_weather`.
///
/// Implemented for [`super::RemoteTables`].
pub trait GameWeatherTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`GameWeatherTableHandle`], which mediates access to the table `game_weather`.
    fn game_weather(&self) -> GameWeatherTableHandle<'_>;
}

impl GameWeatherTableAccess for super::RemoteTables {
    fn game_weather(&self) -> GameWeatherTableHandle<'_> {
        GameWeatherTableHandle {
            imp: self.imp.get_table::<ZoneWeatherChance>("game_weather"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct GameWeatherInsertCallbackId(__sdk::CallbackId);
pub struct GameWeatherDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableLike for GameWeatherTableHandle<'ctx> {
    type Row = ZoneWeatherChance;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = ZoneWeatherChance> + '_ {
        self.imp.iter()
    }
}

impl<'ctx> __sdk::Table for GameWeatherTableHandle<'ctx> {
    type Row = ZoneWeatherChance;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = ZoneWeatherChance> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = GameWeatherInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameWeatherInsertCallbackId {
        GameWeatherInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameWeatherInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = GameWeatherDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameWeatherDeleteCallbackId {
        GameWeatherDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameWeatherDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

impl<'ctx> __sdk::WithInsert for GameWeatherTableHandle<'ctx> {
    type InsertCallbackId = GameWeatherInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameWeatherInsertCallbackId {
        GameWeatherInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameWeatherInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }
}

impl<'ctx> __sdk::WithDelete for GameWeatherTableHandle<'ctx> {
    type DeleteCallbackId = GameWeatherDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameWeatherDeleteCallbackId {
        GameWeatherDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameWeatherDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct GameWeatherUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for GameWeatherTableHandle<'ctx> {
    type UpdateCallbackId = GameWeatherUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameWeatherUpdateCallbackId {
        GameWeatherUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameWeatherUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

impl<'ctx> __sdk::WithUpdate for GameWeatherTableHandle<'ctx> {
    type UpdateCallbackId = GameWeatherUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameWeatherUpdateCallbackId {
        GameWeatherUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameWeatherUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `zone_id` unique index on the table `game_weather`,
/// which allows point queries on the field of the same name
/// via the [`GameWeatherZoneIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_weather().zone_id().find(...)`.
pub struct GameWeatherZoneIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<ZoneWeatherChance, u32>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> GameWeatherTableHandle<'ctx> {
    /// Get a handle on the `zone_id` unique index on the table `game_weather`.
    pub fn zone_id(&self) -> GameWeatherZoneIdUnique<'ctx> {
        GameWeatherZoneIdUnique {
            imp: self.imp.get_unique_constraint::<u32>("zone_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> GameWeatherZoneIdUnique<'ctx> {
    /// Find the subscribed row whose `zone_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u32) -> Option<ZoneWeatherChance> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<ZoneWeatherChance>("game_weather");
    _table.add_unique_constraint::<u32>("zone_id", |row| &row.zone_id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<ZoneWeatherChance>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<ZoneWeatherChance>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `ZoneWeatherChance`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait game_weatherQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `ZoneWeatherChance`.
    fn game_weather(&self) -> __sdk::__query_builder::Table<ZoneWeatherChance>;
}

impl game_weatherQueryTableAccess for __sdk::QueryTableAccessor {
    fn game_weather(&self) -> __sdk::__query_builder::Table<ZoneWeatherChance> {
        __sdk::__query_builder::Table::new("game_weather")
    }
}
