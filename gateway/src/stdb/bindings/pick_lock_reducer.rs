// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct PickLockArgs {
    pub go_guid: u64,
}

impl From<PickLockArgs> for super::Reducer {
    fn from(args: PickLockArgs) -> Self {
        Self::PickLock {
            go_guid: args.go_guid,
        }
    }
}

impl __sdk::InModule for PickLockArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `pick_lock`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait pick_lock {
    /// Request that the remote module invoke the reducer `pick_lock` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`pick_lock:pick_lock_then`] to run a callback after the reducer completes.
    fn pick_lock(&self, go_guid: u64) -> __sdk::Result<()> {
        self.pick_lock_then(go_guid, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `pick_lock` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn pick_lock_then(
        &self,
        go_guid: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl pick_lock for super::RemoteReducers {
    fn pick_lock_then(
        &self,
        go_guid: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(PickLockArgs { go_guid }, callback)
    }
}
