// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::taxi_flight_schedule_type::TaxiFlightSchedule;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AdvanceTaxiFlightArgs {
    pub schedule: TaxiFlightSchedule,
}

impl From<AdvanceTaxiFlightArgs> for super::Reducer {
    fn from(args: AdvanceTaxiFlightArgs) -> Self {
        Self::AdvanceTaxiFlight {
            schedule: args.schedule,
        }
    }
}

impl __sdk::InModule for AdvanceTaxiFlightArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `advance_taxi_flight`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait advance_taxi_flight {
    /// Request that the remote module invoke the reducer `advance_taxi_flight` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`advance_taxi_flight:advance_taxi_flight_then`] to run a callback after the reducer completes.
    fn advance_taxi_flight(&self, schedule: TaxiFlightSchedule) -> __sdk::Result<()> {
        self.advance_taxi_flight_then(schedule, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `advance_taxi_flight` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn advance_taxi_flight_then(
        &self,
        schedule: TaxiFlightSchedule,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl advance_taxi_flight for super::RemoteReducers {
    fn advance_taxi_flight_then(
        &self,
        schedule: TaxiFlightSchedule,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(AdvanceTaxiFlightArgs { schedule }, callback)
    }
}
