// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::session_actor_type::SessionActor;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct GwGuildFeeFinishArgs {
    pub operation_id: u64,
    pub request_actor: SessionActor,
    pub accepted: bool,
}

impl From<GwGuildFeeFinishArgs> for super::Reducer {
    fn from(args: GwGuildFeeFinishArgs) -> Self {
        Self::GwGuildFeeFinish {
            operation_id: args.operation_id,
            request_actor: args.request_actor,
            accepted: args.accepted,
        }
    }
}

impl __sdk::InModule for GwGuildFeeFinishArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `gw_guild_fee_finish`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait gw_guild_fee_finish {
    /// Request that the remote module invoke the reducer `gw_guild_fee_finish` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`gw_guild_fee_finish:gw_guild_fee_finish_then`] to run a callback after the reducer completes.
    fn gw_guild_fee_finish(
        &self,
        operation_id: u64,
        request_actor: SessionActor,
        accepted: bool,
    ) -> __sdk::Result<()> {
        self.gw_guild_fee_finish_then(operation_id, request_actor, accepted, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `gw_guild_fee_finish` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn gw_guild_fee_finish_then(
        &self,
        operation_id: u64,
        request_actor: SessionActor,
        accepted: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl gw_guild_fee_finish for super::RemoteReducers {
    fn gw_guild_fee_finish_then(
        &self,
        operation_id: u64,
        request_actor: SessionActor,
        accepted: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            GwGuildFeeFinishArgs {
                operation_id,
                request_actor,
                accepted,
            },
            callback,
        )
    }
}
