// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DebugStressRelayArgs {
    pub character_guid: u64,
    pub victim_entry: u32,
    pub item_entry: u32,
    pub junk_rows: u32,
}

impl From<DebugStressRelayArgs> for super::Reducer {
    fn from(args: DebugStressRelayArgs) -> Self {
        Self::DebugStressRelay {
            character_guid: args.character_guid,
            victim_entry: args.victim_entry,
            item_entry: args.item_entry,
            junk_rows: args.junk_rows,
        }
    }
}

impl __sdk::InModule for DebugStressRelayArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `debug_stress_relay`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait debug_stress_relay {
    /// Request that the remote module invoke the reducer `debug_stress_relay` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`debug_stress_relay:debug_stress_relay_then`] to run a callback after the reducer completes.
    fn debug_stress_relay(
        &self,
        character_guid: u64,
        victim_entry: u32,
        item_entry: u32,
        junk_rows: u32,
    ) -> __sdk::Result<()> {
        self.debug_stress_relay_then(
            character_guid,
            victim_entry,
            item_entry,
            junk_rows,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `debug_stress_relay` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn debug_stress_relay_then(
        &self,
        character_guid: u64,
        victim_entry: u32,
        item_entry: u32,
        junk_rows: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl debug_stress_relay for super::RemoteReducers {
    fn debug_stress_relay_then(
        &self,
        character_guid: u64,
        victim_entry: u32,
        item_entry: u32,
        junk_rows: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DebugStressRelayArgs {
                character_guid,
                victim_entry,
                item_entry,
                junk_rows,
            },
            callback,
        )
    }
}
