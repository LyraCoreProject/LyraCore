// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::guild_fee_terms_type::GuildFeeTerms;
use super::session_actor_type::SessionActor;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RealmGuildFeeDecideArgs {
    pub operation_id: u64,
    pub request_actor: SessionActor,
    pub terms: GuildFeeTerms,
}

impl From<RealmGuildFeeDecideArgs> for super::Reducer {
    fn from(args: RealmGuildFeeDecideArgs) -> Self {
        Self::RealmGuildFeeDecide {
            operation_id: args.operation_id,
            request_actor: args.request_actor,
            terms: args.terms,
        }
    }
}

impl __sdk::InModule for RealmGuildFeeDecideArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `realm_guild_fee_decide`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait realm_guild_fee_decide {
    /// Request that the remote module invoke the reducer `realm_guild_fee_decide` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`realm_guild_fee_decide:realm_guild_fee_decide_then`] to run a callback after the reducer completes.
    fn realm_guild_fee_decide(
        &self,
        operation_id: u64,
        request_actor: SessionActor,
        terms: GuildFeeTerms,
    ) -> __sdk::Result<()> {
        self.realm_guild_fee_decide_then(operation_id, request_actor, terms, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `realm_guild_fee_decide` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn realm_guild_fee_decide_then(
        &self,
        operation_id: u64,
        request_actor: SessionActor,
        terms: GuildFeeTerms,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl realm_guild_fee_decide for super::RemoteReducers {
    fn realm_guild_fee_decide_then(
        &self,
        operation_id: u64,
        request_actor: SessionActor,
        terms: GuildFeeTerms,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RealmGuildFeeDecideArgs {
                operation_id,
                request_actor,
                terms,
            },
            callback,
        )
    }
}
