// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct PlayerLoginArgs {
    pub character_guid: u64,
}

impl From<PlayerLoginArgs> for super::Reducer {
    fn from(args: PlayerLoginArgs) -> Self {
        Self::PlayerLogin {
            character_guid: args.character_guid,
        }
    }
}

impl __sdk::InModule for PlayerLoginArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `player_login`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait player_login {
    /// Request that the remote module invoke the reducer `player_login` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`player_login:player_login_then`] to run a callback after the reducer completes.
    fn player_login(&self, character_guid: u64) -> __sdk::Result<()> {
        self.player_login_then(character_guid, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `player_login` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn player_login_then(
        &self,
        character_guid: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl player_login for super::RemoteReducers {
    fn player_login_then(
        &self,
        character_guid: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(PlayerLoginArgs { character_guid }, callback)
    }
}
