// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RealmMailTakeItemFenceArgs {
    pub escrow_id: u64,
    pub payee_guid: u64,
    pub mail_id: u64,
    pub expect_entry: u32,
}

impl From<RealmMailTakeItemFenceArgs> for super::Reducer {
    fn from(args: RealmMailTakeItemFenceArgs) -> Self {
        Self::RealmMailTakeItemFence {
            escrow_id: args.escrow_id,
            payee_guid: args.payee_guid,
            mail_id: args.mail_id,
            expect_entry: args.expect_entry,
        }
    }
}

impl __sdk::InModule for RealmMailTakeItemFenceArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `realm_mail_take_item_fence`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait realm_mail_take_item_fence {
    /// Request that the remote module invoke the reducer `realm_mail_take_item_fence` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`realm_mail_take_item_fence:realm_mail_take_item_fence_then`] to run a callback after the reducer completes.
    fn realm_mail_take_item_fence(
        &self,
        escrow_id: u64,
        payee_guid: u64,
        mail_id: u64,
        expect_entry: u32,
    ) -> __sdk::Result<()> {
        self.realm_mail_take_item_fence_then(
            escrow_id,
            payee_guid,
            mail_id,
            expect_entry,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `realm_mail_take_item_fence` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn realm_mail_take_item_fence_then(
        &self,
        escrow_id: u64,
        payee_guid: u64,
        mail_id: u64,
        expect_entry: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl realm_mail_take_item_fence for super::RemoteReducers {
    fn realm_mail_take_item_fence_then(
        &self,
        escrow_id: u64,
        payee_guid: u64,
        mail_id: u64,
        expect_entry: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RealmMailTakeItemFenceArgs {
                escrow_id,
                payee_guid,
                mail_id,
                expect_entry,
            },
            callback,
        )
    }
}
