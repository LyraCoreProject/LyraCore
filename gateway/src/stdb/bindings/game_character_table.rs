// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::character_type::Character;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `game_character`.
///
/// Obtain a handle from the [`GameCharacterTableAccess::game_character`] method on [`super::RemoteTables`],
/// like `ctx.db.game_character()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_character().on_insert(...)`.
pub struct GameCharacterTableHandle<'ctx> {
    imp: __sdk::TableHandle<Character>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `game_character`.
///
/// Implemented for [`super::RemoteTables`].
pub trait GameCharacterTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`GameCharacterTableHandle`], which mediates access to the table `game_character`.
    fn game_character(&self) -> GameCharacterTableHandle<'_>;
}

impl GameCharacterTableAccess for super::RemoteTables {
    fn game_character(&self) -> GameCharacterTableHandle<'_> {
        GameCharacterTableHandle {
            imp: self.imp.get_table::<Character>("game_character"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct GameCharacterInsertCallbackId(__sdk::CallbackId);
pub struct GameCharacterDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for GameCharacterTableHandle<'ctx> {
    type Row = Character;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = Character> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = GameCharacterInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameCharacterInsertCallbackId {
        GameCharacterInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameCharacterInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = GameCharacterDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameCharacterDeleteCallbackId {
        GameCharacterDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameCharacterDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct GameCharacterUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for GameCharacterTableHandle<'ctx> {
    type UpdateCallbackId = GameCharacterUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameCharacterUpdateCallbackId {
        GameCharacterUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameCharacterUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `guid` unique index on the table `game_character`,
/// which allows point queries on the field of the same name
/// via the [`GameCharacterGuidUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_character().guid().find(...)`.
pub struct GameCharacterGuidUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<Character, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> GameCharacterTableHandle<'ctx> {
    /// Get a handle on the `guid` unique index on the table `game_character`.
    pub fn guid(&self) -> GameCharacterGuidUnique<'ctx> {
        GameCharacterGuidUnique {
            imp: self.imp.get_unique_constraint::<u64>("guid"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> GameCharacterGuidUnique<'ctx> {
    /// Find the subscribed row whose `guid` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<Character> {
        self.imp.find(col_val)
    }
}

/// Access to the `name` unique index on the table `game_character`,
/// which allows point queries on the field of the same name
/// via the [`GameCharacterNameUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_character().name().find(...)`.
pub struct GameCharacterNameUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<Character, String>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> GameCharacterTableHandle<'ctx> {
    /// Get a handle on the `name` unique index on the table `game_character`.
    pub fn name(&self) -> GameCharacterNameUnique<'ctx> {
        GameCharacterNameUnique {
            imp: self.imp.get_unique_constraint::<String>("name"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> GameCharacterNameUnique<'ctx> {
    /// Find the subscribed row whose `name` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &String) -> Option<Character> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<Character>("game_character");
    _table.add_unique_constraint::<u64>("guid", |row| &row.guid);
    _table.add_unique_constraint::<String>("name", |row| &row.name);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<Character>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<Character>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `Character`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait game_characterQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `Character`.
    fn game_character(&self) -> __sdk::__query_builder::Table<Character>;
}

impl game_characterQueryTableAccess for __sdk::QueryTableAccessor {
    fn game_character(&self) -> __sdk::__query_builder::Table<Character> {
        __sdk::__query_builder::Table::new("game_character")
    }
}
