// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct LootMasterGiveArgs {
    pub corpse_guid: u64,
    pub loot_slot: u8,
    pub target_guid: u64,
}

impl From<LootMasterGiveArgs> for super::Reducer {
    fn from(args: LootMasterGiveArgs) -> Self {
        Self::LootMasterGive {
            corpse_guid: args.corpse_guid,
            loot_slot: args.loot_slot,
            target_guid: args.target_guid,
        }
    }
}

impl __sdk::InModule for LootMasterGiveArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `loot_master_give`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait loot_master_give {
    /// Request that the remote module invoke the reducer `loot_master_give` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`loot_master_give:loot_master_give_then`] to run a callback after the reducer completes.
    fn loot_master_give(
        &self,
        corpse_guid: u64,
        loot_slot: u8,
        target_guid: u64,
    ) -> __sdk::Result<()> {
        self.loot_master_give_then(corpse_guid, loot_slot, target_guid, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `loot_master_give` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn loot_master_give_then(
        &self,
        corpse_guid: u64,
        loot_slot: u8,
        target_guid: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl loot_master_give for super::RemoteReducers {
    fn loot_master_give_then(
        &self,
        corpse_guid: u64,
        loot_slot: u8,
        target_guid: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            LootMasterGiveArgs {
                corpse_guid,
                loot_slot,
                target_guid,
            },
            callback,
        )
    }
}
