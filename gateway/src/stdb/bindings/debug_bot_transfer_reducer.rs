// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DebugBotTransferArgs {
    pub character_guid: u64,
    pub map_id: u32,
    pub instance_id: u64,
    pub x: f32,
    pub y: f32,
    pub z: f32,
    pub o: f32,
    pub reason: String,
}

impl From<DebugBotTransferArgs> for super::Reducer {
    fn from(args: DebugBotTransferArgs) -> Self {
        Self::DebugBotTransfer {
            character_guid: args.character_guid,
            map_id: args.map_id,
            instance_id: args.instance_id,
            x: args.x,
            y: args.y,
            z: args.z,
            o: args.o,
            reason: args.reason,
        }
    }
}

impl __sdk::InModule for DebugBotTransferArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `debug_bot_transfer`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait debug_bot_transfer {
    /// Request that the remote module invoke the reducer `debug_bot_transfer` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`debug_bot_transfer:debug_bot_transfer_then`] to run a callback after the reducer completes.
    fn debug_bot_transfer(
        &self,
        character_guid: u64,
        map_id: u32,
        instance_id: u64,
        x: f32,
        y: f32,
        z: f32,
        o: f32,
        reason: String,
    ) -> __sdk::Result<()> {
        self.debug_bot_transfer_then(
            character_guid,
            map_id,
            instance_id,
            x,
            y,
            z,
            o,
            reason,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `debug_bot_transfer` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn debug_bot_transfer_then(
        &self,
        character_guid: u64,
        map_id: u32,
        instance_id: u64,
        x: f32,
        y: f32,
        z: f32,
        o: f32,
        reason: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl debug_bot_transfer for super::RemoteReducers {
    fn debug_bot_transfer_then(
        &self,
        character_guid: u64,
        map_id: u32,
        instance_id: u64,
        x: f32,
        y: f32,
        z: f32,
        o: f32,
        reason: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DebugBotTransferArgs {
                character_guid,
                map_id,
                instance_id,
                x,
                y,
                z,
                o,
                reason,
            },
            callback,
        )
    }
}
