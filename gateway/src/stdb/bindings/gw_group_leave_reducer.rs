// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::session_actor_type::SessionActor;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct GwGroupLeaveArgs {
    pub request_actor: SessionActor,
}

impl From<GwGroupLeaveArgs> for super::Reducer {
    fn from(args: GwGroupLeaveArgs) -> Self {
        Self::GwGroupLeave {
            request_actor: args.request_actor,
        }
    }
}

impl __sdk::InModule for GwGroupLeaveArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `gw_group_leave`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait gw_group_leave {
    /// Request that the remote module invoke the reducer `gw_group_leave` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`gw_group_leave:gw_group_leave_then`] to run a callback after the reducer completes.
    fn gw_group_leave(&self, request_actor: SessionActor) -> __sdk::Result<()> {
        self.gw_group_leave_then(request_actor, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `gw_group_leave` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn gw_group_leave_then(
        &self,
        request_actor: SessionActor,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl gw_group_leave for super::RemoteReducers {
    fn gw_group_leave_then(
        &self,
        request_actor: SessionActor,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(GwGroupLeaveArgs { request_actor }, callback)
    }
}
