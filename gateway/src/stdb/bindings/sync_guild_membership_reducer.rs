// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SyncGuildMembershipArgs {
    pub character_guid: u64,
    pub guild_id: u64,
    pub guild_rank: u32,
}

impl From<SyncGuildMembershipArgs> for super::Reducer {
    fn from(args: SyncGuildMembershipArgs) -> Self {
        Self::SyncGuildMembership {
            character_guid: args.character_guid,
            guild_id: args.guild_id,
            guild_rank: args.guild_rank,
        }
    }
}

impl __sdk::InModule for SyncGuildMembershipArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `sync_guild_membership`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait sync_guild_membership {
    /// Request that the remote module invoke the reducer `sync_guild_membership` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`sync_guild_membership:sync_guild_membership_then`] to run a callback after the reducer completes.
    fn sync_guild_membership(
        &self,
        character_guid: u64,
        guild_id: u64,
        guild_rank: u32,
    ) -> __sdk::Result<()> {
        self.sync_guild_membership_then(character_guid, guild_id, guild_rank, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `sync_guild_membership` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn sync_guild_membership_then(
        &self,
        character_guid: u64,
        guild_id: u64,
        guild_rank: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl sync_guild_membership for super::RemoteReducers {
    fn sync_guild_membership_then(
        &self,
        character_guid: u64,
        guild_id: u64,
        guild_rank: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SyncGuildMembershipArgs {
                character_guid,
                guild_id,
                guild_rank,
            },
            callback,
        )
    }
}
