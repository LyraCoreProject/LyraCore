// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct MarkBotTransferArrivalReadyArgs {
    pub intent_id: u64,
    pub bot_guid: u64,
    pub controller_generation: u64,
    pub claim_token: u64,
}

impl From<MarkBotTransferArrivalReadyArgs> for super::Reducer {
    fn from(args: MarkBotTransferArrivalReadyArgs) -> Self {
        Self::MarkBotTransferArrivalReady {
            intent_id: args.intent_id,
            bot_guid: args.bot_guid,
            controller_generation: args.controller_generation,
            claim_token: args.claim_token,
        }
    }
}

impl __sdk::InModule for MarkBotTransferArrivalReadyArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `mark_bot_transfer_arrival_ready`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait mark_bot_transfer_arrival_ready {
    /// Request that the remote module invoke the reducer `mark_bot_transfer_arrival_ready` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`mark_bot_transfer_arrival_ready:mark_bot_transfer_arrival_ready_then`] to run a callback after the reducer completes.
    fn mark_bot_transfer_arrival_ready(
        &self,
        intent_id: u64,
        bot_guid: u64,
        controller_generation: u64,
        claim_token: u64,
    ) -> __sdk::Result<()> {
        self.mark_bot_transfer_arrival_ready_then(
            intent_id,
            bot_guid,
            controller_generation,
            claim_token,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `mark_bot_transfer_arrival_ready` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn mark_bot_transfer_arrival_ready_then(
        &self,
        intent_id: u64,
        bot_guid: u64,
        controller_generation: u64,
        claim_token: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl mark_bot_transfer_arrival_ready for super::RemoteReducers {
    fn mark_bot_transfer_arrival_ready_then(
        &self,
        intent_id: u64,
        bot_guid: u64,
        controller_generation: u64,
        claim_token: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            MarkBotTransferArrivalReadyArgs {
                intent_id,
                bot_guid,
                controller_generation,
                claim_token,
            },
            callback,
        )
    }
}
