// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ClaimBotTransferIntentArgs {
    pub intent_id: u64,
    pub bot_guid: u64,
    pub controller_generation: u64,
    pub claim_token: u64,
}

impl From<ClaimBotTransferIntentArgs> for super::Reducer {
    fn from(args: ClaimBotTransferIntentArgs) -> Self {
        Self::ClaimBotTransferIntent {
            intent_id: args.intent_id,
            bot_guid: args.bot_guid,
            controller_generation: args.controller_generation,
            claim_token: args.claim_token,
        }
    }
}

impl __sdk::InModule for ClaimBotTransferIntentArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `claim_bot_transfer_intent`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait claim_bot_transfer_intent {
    /// Request that the remote module invoke the reducer `claim_bot_transfer_intent` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`claim_bot_transfer_intent:claim_bot_transfer_intent_then`] to run a callback after the reducer completes.
    fn claim_bot_transfer_intent(
        &self,
        intent_id: u64,
        bot_guid: u64,
        controller_generation: u64,
        claim_token: u64,
    ) -> __sdk::Result<()> {
        self.claim_bot_transfer_intent_then(
            intent_id,
            bot_guid,
            controller_generation,
            claim_token,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `claim_bot_transfer_intent` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn claim_bot_transfer_intent_then(
        &self,
        intent_id: u64,
        bot_guid: u64,
        controller_generation: u64,
        claim_token: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl claim_bot_transfer_intent for super::RemoteReducers {
    fn claim_bot_transfer_intent_then(
        &self,
        intent_id: u64,
        bot_guid: u64,
        controller_generation: u64,
        claim_token: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ClaimBotTransferIntentArgs {
                intent_id,
                bot_guid,
                controller_generation,
                claim_token,
            },
            callback,
        )
    }
}
