// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::realm_type::Realm;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `game_realm`.
///
/// Obtain a handle from the [`GameRealmTableAccess::game_realm`] method on [`super::RemoteTables`],
/// like `ctx.db.game_realm()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_realm().on_insert(...)`.
pub struct GameRealmTableHandle<'ctx> {
    imp: __sdk::TableHandle<Realm>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

/// Lifetime-aware accessor marker for the table `game_realm`.
pub struct GameRealmTableAccessor;

impl __sdk::TableAccessor<super::RemoteTables> for GameRealmTableAccessor {
    type Row = Realm;
    type Handle<'db> = GameRealmTableHandle<'db>;

    fn get<'db>(db: &'db super::RemoteTables) -> Self::Handle<'db> {
        db.game_realm()
    }
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `game_realm`.
///
/// Implemented for [`super::RemoteTables`].
pub trait GameRealmTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`GameRealmTableHandle`], which mediates access to the table `game_realm`.
    fn game_realm(&self) -> GameRealmTableHandle<'_>;
}

impl GameRealmTableAccess for super::RemoteTables {
    fn game_realm(&self) -> GameRealmTableHandle<'_> {
        GameRealmTableHandle {
            imp: self.imp.get_table::<Realm>("game_realm"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct GameRealmInsertCallbackId(__sdk::CallbackId);
pub struct GameRealmDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableLike for GameRealmTableHandle<'ctx> {
    type Row = Realm;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = Realm> + '_ {
        self.imp.iter()
    }
}

impl<'ctx> __sdk::Table for GameRealmTableHandle<'ctx> {
    type Row = Realm;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = Realm> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = GameRealmInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameRealmInsertCallbackId {
        GameRealmInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameRealmInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = GameRealmDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameRealmDeleteCallbackId {
        GameRealmDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameRealmDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

impl<'ctx> __sdk::WithInsert for GameRealmTableHandle<'ctx> {
    type InsertCallbackId = GameRealmInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameRealmInsertCallbackId {
        GameRealmInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameRealmInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }
}

impl<'ctx> __sdk::WithDelete for GameRealmTableHandle<'ctx> {
    type DeleteCallbackId = GameRealmDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameRealmDeleteCallbackId {
        GameRealmDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameRealmDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct GameRealmUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for GameRealmTableHandle<'ctx> {
    type UpdateCallbackId = GameRealmUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameRealmUpdateCallbackId {
        GameRealmUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameRealmUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

impl<'ctx> __sdk::WithUpdate for GameRealmTableHandle<'ctx> {
    type UpdateCallbackId = GameRealmUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameRealmUpdateCallbackId {
        GameRealmUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameRealmUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `game_realm`,
/// which allows point queries on the field of the same name
/// via the [`GameRealmIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_realm().id().find(...)`.
pub struct GameRealmIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<Realm, u8>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> GameRealmTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `game_realm`.
    pub fn id(&self) -> GameRealmIdUnique<'ctx> {
        GameRealmIdUnique {
            imp: self.imp.get_unique_constraint::<u8>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> GameRealmIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u8) -> Option<Realm> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<Realm>("game_realm");
    _table.add_unique_constraint::<u8>("id", |row| &row.id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<Realm>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<Realm>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `Realm`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait game_realmQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `Realm`.
    fn game_realm(&self) -> __sdk::__query_builder::Table<Realm>;
}

impl game_realmQueryTableAccess for __sdk::QueryTableAccessor {
    fn game_realm(&self) -> __sdk::__query_builder::Table<Realm> {
        __sdk::__query_builder::Table::new("game_realm")
    }
}
