// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct FinalizeVmapNavCoverageArgs {
    pub generation_id: u64,
}

impl From<FinalizeVmapNavCoverageArgs> for super::Reducer {
    fn from(args: FinalizeVmapNavCoverageArgs) -> Self {
        Self::FinalizeVmapNavCoverage {
            generation_id: args.generation_id,
        }
    }
}

impl __sdk::InModule for FinalizeVmapNavCoverageArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `finalize_vmap_nav_coverage`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait finalize_vmap_nav_coverage {
    /// Request that the remote module invoke the reducer `finalize_vmap_nav_coverage` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`finalize_vmap_nav_coverage:finalize_vmap_nav_coverage_then`] to run a callback after the reducer completes.
    fn finalize_vmap_nav_coverage(&self, generation_id: u64) -> __sdk::Result<()> {
        self.finalize_vmap_nav_coverage_then(generation_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `finalize_vmap_nav_coverage` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn finalize_vmap_nav_coverage_then(
        &self,
        generation_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl finalize_vmap_nav_coverage for super::RemoteReducers {
    fn finalize_vmap_nav_coverage_then(
        &self,
        generation_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(FinalizeVmapNavCoverageArgs { generation_id }, callback)
    }
}
