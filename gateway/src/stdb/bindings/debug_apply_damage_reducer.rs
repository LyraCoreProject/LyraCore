// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DebugApplyDamageArgs {
    pub target_guid: u64,
    pub amount: u32,
    pub attacker_guid: u64,
}

impl From<DebugApplyDamageArgs> for super::Reducer {
    fn from(args: DebugApplyDamageArgs) -> Self {
        Self::DebugApplyDamage {
            target_guid: args.target_guid,
            amount: args.amount,
            attacker_guid: args.attacker_guid,
        }
    }
}

impl __sdk::InModule for DebugApplyDamageArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `debug_apply_damage`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait debug_apply_damage {
    /// Request that the remote module invoke the reducer `debug_apply_damage` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`debug_apply_damage:debug_apply_damage_then`] to run a callback after the reducer completes.
    fn debug_apply_damage(
        &self,
        target_guid: u64,
        amount: u32,
        attacker_guid: u64,
    ) -> __sdk::Result<()> {
        self.debug_apply_damage_then(target_guid, amount, attacker_guid, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `debug_apply_damage` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn debug_apply_damage_then(
        &self,
        target_guid: u64,
        amount: u32,
        attacker_guid: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl debug_apply_damage for super::RemoteReducers {
    fn debug_apply_damage_then(
        &self,
        target_guid: u64,
        amount: u32,
        attacker_guid: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DebugApplyDamageArgs {
                target_guid,
                amount,
                attacker_guid,
            },
            callback,
        )
    }
}
