// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::corpse_loot_eligible_type::CorpseLootEligible;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `game_corpse_loot_eligible`.
///
/// Obtain a handle from the [`GameCorpseLootEligibleTableAccess::game_corpse_loot_eligible`] method on [`super::RemoteTables`],
/// like `ctx.db.game_corpse_loot_eligible()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_corpse_loot_eligible().on_insert(...)`.
pub struct GameCorpseLootEligibleTableHandle<'ctx> {
    imp: __sdk::TableHandle<CorpseLootEligible>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

/// Lifetime-aware accessor marker for the table `game_corpse_loot_eligible`.
pub struct GameCorpseLootEligibleTableAccessor;

impl __sdk::TableAccessor<super::RemoteTables> for GameCorpseLootEligibleTableAccessor {
    type Row = CorpseLootEligible;
    type Handle<'db> = GameCorpseLootEligibleTableHandle<'db>;

    fn get<'db>(db: &'db super::RemoteTables) -> Self::Handle<'db> {
        db.game_corpse_loot_eligible()
    }
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `game_corpse_loot_eligible`.
///
/// Implemented for [`super::RemoteTables`].
pub trait GameCorpseLootEligibleTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`GameCorpseLootEligibleTableHandle`], which mediates access to the table `game_corpse_loot_eligible`.
    fn game_corpse_loot_eligible(&self) -> GameCorpseLootEligibleTableHandle<'_>;
}

impl GameCorpseLootEligibleTableAccess for super::RemoteTables {
    fn game_corpse_loot_eligible(&self) -> GameCorpseLootEligibleTableHandle<'_> {
        GameCorpseLootEligibleTableHandle {
            imp: self
                .imp
                .get_table::<CorpseLootEligible>("game_corpse_loot_eligible"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct GameCorpseLootEligibleInsertCallbackId(__sdk::CallbackId);
pub struct GameCorpseLootEligibleDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableLike for GameCorpseLootEligibleTableHandle<'ctx> {
    type Row = CorpseLootEligible;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = CorpseLootEligible> + '_ {
        self.imp.iter()
    }
}

impl<'ctx> __sdk::Table for GameCorpseLootEligibleTableHandle<'ctx> {
    type Row = CorpseLootEligible;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = CorpseLootEligible> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = GameCorpseLootEligibleInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameCorpseLootEligibleInsertCallbackId {
        GameCorpseLootEligibleInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameCorpseLootEligibleInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = GameCorpseLootEligibleDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameCorpseLootEligibleDeleteCallbackId {
        GameCorpseLootEligibleDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameCorpseLootEligibleDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

impl<'ctx> __sdk::WithInsert for GameCorpseLootEligibleTableHandle<'ctx> {
    type InsertCallbackId = GameCorpseLootEligibleInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameCorpseLootEligibleInsertCallbackId {
        GameCorpseLootEligibleInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameCorpseLootEligibleInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }
}

impl<'ctx> __sdk::WithDelete for GameCorpseLootEligibleTableHandle<'ctx> {
    type DeleteCallbackId = GameCorpseLootEligibleDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameCorpseLootEligibleDeleteCallbackId {
        GameCorpseLootEligibleDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameCorpseLootEligibleDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct GameCorpseLootEligibleUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for GameCorpseLootEligibleTableHandle<'ctx> {
    type UpdateCallbackId = GameCorpseLootEligibleUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameCorpseLootEligibleUpdateCallbackId {
        GameCorpseLootEligibleUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameCorpseLootEligibleUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

impl<'ctx> __sdk::WithUpdate for GameCorpseLootEligibleTableHandle<'ctx> {
    type UpdateCallbackId = GameCorpseLootEligibleUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameCorpseLootEligibleUpdateCallbackId {
        GameCorpseLootEligibleUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameCorpseLootEligibleUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `game_corpse_loot_eligible`,
/// which allows point queries on the field of the same name
/// via the [`GameCorpseLootEligibleIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_corpse_loot_eligible().id().find(...)`.
pub struct GameCorpseLootEligibleIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<CorpseLootEligible, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> GameCorpseLootEligibleTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `game_corpse_loot_eligible`.
    pub fn id(&self) -> GameCorpseLootEligibleIdUnique<'ctx> {
        GameCorpseLootEligibleIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> GameCorpseLootEligibleIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<CorpseLootEligible> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<CorpseLootEligible>("game_corpse_loot_eligible");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<CorpseLootEligible>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<CorpseLootEligible>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `CorpseLootEligible`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait game_corpse_loot_eligibleQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `CorpseLootEligible`.
    fn game_corpse_loot_eligible(&self) -> __sdk::__query_builder::Table<CorpseLootEligible>;
}

impl game_corpse_loot_eligibleQueryTableAccess for __sdk::QueryTableAccessor {
    fn game_corpse_loot_eligible(&self) -> __sdk::__query_builder::Table<CorpseLootEligible> {
        __sdk::__query_builder::Table::new("game_corpse_loot_eligible")
    }
}
