// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RealmLootOpArgs {
    pub op: u8,
    pub corpse_guid: u64,
    pub slot: u8,
    pub item_entry: u32,
    pub actor_guid: u64,
    pub vote: u8,
    pub deadline_micros: i64,
    pub recipients: Vec<u64>,
}

impl From<RealmLootOpArgs> for super::Reducer {
    fn from(args: RealmLootOpArgs) -> Self {
        Self::RealmLootOp {
            op: args.op,
            corpse_guid: args.corpse_guid,
            slot: args.slot,
            item_entry: args.item_entry,
            actor_guid: args.actor_guid,
            vote: args.vote,
            deadline_micros: args.deadline_micros,
            recipients: args.recipients,
        }
    }
}

impl __sdk::InModule for RealmLootOpArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `realm_loot_op`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait realm_loot_op {
    /// Request that the remote module invoke the reducer `realm_loot_op` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`realm_loot_op:realm_loot_op_then`] to run a callback after the reducer completes.
    fn realm_loot_op(
        &self,
        op: u8,
        corpse_guid: u64,
        slot: u8,
        item_entry: u32,
        actor_guid: u64,
        vote: u8,
        deadline_micros: i64,
        recipients: Vec<u64>,
    ) -> __sdk::Result<()> {
        self.realm_loot_op_then(
            op,
            corpse_guid,
            slot,
            item_entry,
            actor_guid,
            vote,
            deadline_micros,
            recipients,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `realm_loot_op` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn realm_loot_op_then(
        &self,
        op: u8,
        corpse_guid: u64,
        slot: u8,
        item_entry: u32,
        actor_guid: u64,
        vote: u8,
        deadline_micros: i64,
        recipients: Vec<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl realm_loot_op for super::RemoteReducers {
    fn realm_loot_op_then(
        &self,
        op: u8,
        corpse_guid: u64,
        slot: u8,
        item_entry: u32,
        actor_guid: u64,
        vote: u8,
        deadline_micros: i64,
        recipients: Vec<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RealmLootOpArgs {
                op,
                corpse_guid,
                slot,
                item_entry,
                actor_guid,
                vote,
                deadline_micros,
                recipients,
            },
            callback,
        )
    }
}
