// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ClientCommandArgs {
    pub cmd: String,
    pub payload: String,
}

impl From<ClientCommandArgs> for super::Reducer {
    fn from(args: ClientCommandArgs) -> Self {
        Self::ClientCommand {
            cmd: args.cmd,
            payload: args.payload,
        }
    }
}

impl __sdk::InModule for ClientCommandArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `client_command`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait client_command {
    /// Request that the remote module invoke the reducer `client_command` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`client_command:client_command_then`] to run a callback after the reducer completes.
    fn client_command(&self, cmd: String, payload: String) -> __sdk::Result<()> {
        self.client_command_then(cmd, payload, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `client_command` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn client_command_then(
        &self,
        cmd: String,
        payload: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl client_command for super::RemoteReducers {
    fn client_command_then(
        &self,
        cmd: String,
        payload: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(ClientCommandArgs { cmd, payload }, callback)
    }
}
