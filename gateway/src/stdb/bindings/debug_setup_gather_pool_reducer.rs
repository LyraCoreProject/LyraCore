// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DebugSetupGatherPoolArgs {
    pub pool_id: u32,
    pub max_active: u32,
    pub in_place: bool,
    pub members: String,
}

impl From<DebugSetupGatherPoolArgs> for super::Reducer {
    fn from(args: DebugSetupGatherPoolArgs) -> Self {
        Self::DebugSetupGatherPool {
            pool_id: args.pool_id,
            max_active: args.max_active,
            in_place: args.in_place,
            members: args.members,
        }
    }
}

impl __sdk::InModule for DebugSetupGatherPoolArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `debug_setup_gather_pool`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait debug_setup_gather_pool {
    /// Request that the remote module invoke the reducer `debug_setup_gather_pool` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`debug_setup_gather_pool:debug_setup_gather_pool_then`] to run a callback after the reducer completes.
    fn debug_setup_gather_pool(
        &self,
        pool_id: u32,
        max_active: u32,
        in_place: bool,
        members: String,
    ) -> __sdk::Result<()> {
        self.debug_setup_gather_pool_then(pool_id, max_active, in_place, members, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `debug_setup_gather_pool` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn debug_setup_gather_pool_then(
        &self,
        pool_id: u32,
        max_active: u32,
        in_place: bool,
        members: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl debug_setup_gather_pool for super::RemoteReducers {
    fn debug_setup_gather_pool_then(
        &self,
        pool_id: u32,
        max_active: u32,
        in_place: bool,
        members: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DebugSetupGatherPoolArgs {
                pool_id,
                max_active,
                in_place,
                members,
            },
            callback,
        )
    }
}
