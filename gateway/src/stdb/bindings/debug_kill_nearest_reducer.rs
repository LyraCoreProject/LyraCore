// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DebugKillNearestArgs {
    pub killer_guid: u64,
    pub creature_entry: u32,
}

impl From<DebugKillNearestArgs> for super::Reducer {
    fn from(args: DebugKillNearestArgs) -> Self {
        Self::DebugKillNearest {
            killer_guid: args.killer_guid,
            creature_entry: args.creature_entry,
        }
    }
}

impl __sdk::InModule for DebugKillNearestArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `debug_kill_nearest`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait debug_kill_nearest {
    /// Request that the remote module invoke the reducer `debug_kill_nearest` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`debug_kill_nearest:debug_kill_nearest_then`] to run a callback after the reducer completes.
    fn debug_kill_nearest(&self, killer_guid: u64, creature_entry: u32) -> __sdk::Result<()> {
        self.debug_kill_nearest_then(killer_guid, creature_entry, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `debug_kill_nearest` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn debug_kill_nearest_then(
        &self,
        killer_guid: u64,
        creature_entry: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl debug_kill_nearest for super::RemoteReducers {
    fn debug_kill_nearest_then(
        &self,
        killer_guid: u64,
        creature_entry: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DebugKillNearestArgs {
                killer_guid,
                creature_entry,
            },
            callback,
        )
    }
}
