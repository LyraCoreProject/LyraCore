// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct PlayerbotsTravelArgs {
    pub bot_guid: u64,
    pub dest_entry: u32,
}

impl From<PlayerbotsTravelArgs> for super::Reducer {
    fn from(args: PlayerbotsTravelArgs) -> Self {
        Self::PlayerbotsTravel {
            bot_guid: args.bot_guid,
            dest_entry: args.dest_entry,
        }
    }
}

impl __sdk::InModule for PlayerbotsTravelArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `playerbots_travel`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait playerbots_travel {
    /// Request that the remote module invoke the reducer `playerbots_travel` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`playerbots_travel:playerbots_travel_then`] to run a callback after the reducer completes.
    fn playerbots_travel(&self, bot_guid: u64, dest_entry: u32) -> __sdk::Result<()> {
        self.playerbots_travel_then(bot_guid, dest_entry, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `playerbots_travel` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn playerbots_travel_then(
        &self,
        bot_guid: u64,
        dest_entry: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl playerbots_travel for super::RemoteReducers {
    fn playerbots_travel_then(
        &self,
        bot_guid: u64,
        dest_entry: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            PlayerbotsTravelArgs {
                bot_guid,
                dest_entry,
            },
            callback,
        )
    }
}
