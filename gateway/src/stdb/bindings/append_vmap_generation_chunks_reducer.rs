// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AppendVmapGenerationChunksArgs {
    pub generation_id: u64,
    pub packed: String,
}

impl From<AppendVmapGenerationChunksArgs> for super::Reducer {
    fn from(args: AppendVmapGenerationChunksArgs) -> Self {
        Self::AppendVmapGenerationChunks {
            generation_id: args.generation_id,
            packed: args.packed,
        }
    }
}

impl __sdk::InModule for AppendVmapGenerationChunksArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `append_vmap_generation_chunks`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait append_vmap_generation_chunks {
    /// Request that the remote module invoke the reducer `append_vmap_generation_chunks` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`append_vmap_generation_chunks:append_vmap_generation_chunks_then`] to run a callback after the reducer completes.
    fn append_vmap_generation_chunks(
        &self,
        generation_id: u64,
        packed: String,
    ) -> __sdk::Result<()> {
        self.append_vmap_generation_chunks_then(generation_id, packed, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `append_vmap_generation_chunks` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn append_vmap_generation_chunks_then(
        &self,
        generation_id: u64,
        packed: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl append_vmap_generation_chunks for super::RemoteReducers {
    fn append_vmap_generation_chunks_then(
        &self,
        generation_id: u64,
        packed: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AppendVmapGenerationChunksArgs {
                generation_id,
                packed,
            },
            callback,
        )
    }
}
