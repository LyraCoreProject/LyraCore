// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct PartyChatArgs {
    pub text: String,
}

impl From<PartyChatArgs> for super::Reducer {
    fn from(args: PartyChatArgs) -> Self {
        Self::PartyChat { text: args.text }
    }
}

impl __sdk::InModule for PartyChatArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `party_chat`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait party_chat {
    /// Request that the remote module invoke the reducer `party_chat` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`party_chat:party_chat_then`] to run a callback after the reducer completes.
    fn party_chat(&self, text: String) -> __sdk::Result<()> {
        self.party_chat_then(text, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `party_chat` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn party_chat_then(
        &self,
        text: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl party_chat for super::RemoteReducers {
    fn party_chat_then(
        &self,
        text: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(PartyChatArgs { text }, callback)
    }
}
