// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::session_actor_type::SessionActor;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct GwMailGrantLetterArgs {
    pub request_actor: SessionActor,
    pub item_text_id: u32,
}

impl From<GwMailGrantLetterArgs> for super::Reducer {
    fn from(args: GwMailGrantLetterArgs) -> Self {
        Self::GwMailGrantLetter {
            request_actor: args.request_actor,
            item_text_id: args.item_text_id,
        }
    }
}

impl __sdk::InModule for GwMailGrantLetterArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `gw_mail_grant_letter`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait gw_mail_grant_letter {
    /// Request that the remote module invoke the reducer `gw_mail_grant_letter` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`gw_mail_grant_letter:gw_mail_grant_letter_then`] to run a callback after the reducer completes.
    fn gw_mail_grant_letter(
        &self,
        request_actor: SessionActor,
        item_text_id: u32,
    ) -> __sdk::Result<()> {
        self.gw_mail_grant_letter_then(request_actor, item_text_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `gw_mail_grant_letter` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn gw_mail_grant_letter_then(
        &self,
        request_actor: SessionActor,
        item_text_id: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl gw_mail_grant_letter for super::RemoteReducers {
    fn gw_mail_grant_letter_then(
        &self,
        request_actor: SessionActor,
        item_text_id: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            GwMailGrantLetterArgs {
                request_actor,
                item_text_id,
            },
            callback,
        )
    }
}
