// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::taxi_passenger_spline_type::TaxiPassengerSpline;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `game_taxi_passenger_spline`.
///
/// Obtain a handle from the [`GameTaxiPassengerSplineTableAccess::game_taxi_passenger_spline`] method on [`super::RemoteTables`],
/// like `ctx.db.game_taxi_passenger_spline()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_taxi_passenger_spline().on_insert(...)`.
pub struct GameTaxiPassengerSplineTableHandle<'ctx> {
    imp: __sdk::TableHandle<TaxiPassengerSpline>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

/// Lifetime-aware accessor marker for the table `game_taxi_passenger_spline`.
pub struct GameTaxiPassengerSplineTableAccessor;

impl __sdk::TableAccessor<super::RemoteTables> for GameTaxiPassengerSplineTableAccessor {
    type Row = TaxiPassengerSpline;
    type Handle<'db> = GameTaxiPassengerSplineTableHandle<'db>;

    fn get<'db>(db: &'db super::RemoteTables) -> Self::Handle<'db> {
        db.game_taxi_passenger_spline()
    }
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `game_taxi_passenger_spline`.
///
/// Implemented for [`super::RemoteTables`].
pub trait GameTaxiPassengerSplineTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`GameTaxiPassengerSplineTableHandle`], which mediates access to the table `game_taxi_passenger_spline`.
    fn game_taxi_passenger_spline(&self) -> GameTaxiPassengerSplineTableHandle<'_>;
}

impl GameTaxiPassengerSplineTableAccess for super::RemoteTables {
    fn game_taxi_passenger_spline(&self) -> GameTaxiPassengerSplineTableHandle<'_> {
        GameTaxiPassengerSplineTableHandle {
            imp: self
                .imp
                .get_table::<TaxiPassengerSpline>("game_taxi_passenger_spline"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct GameTaxiPassengerSplineInsertCallbackId(__sdk::CallbackId);
pub struct GameTaxiPassengerSplineDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableLike for GameTaxiPassengerSplineTableHandle<'ctx> {
    type Row = TaxiPassengerSpline;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TaxiPassengerSpline> + '_ {
        self.imp.iter()
    }
}

impl<'ctx> __sdk::Table for GameTaxiPassengerSplineTableHandle<'ctx> {
    type Row = TaxiPassengerSpline;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TaxiPassengerSpline> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = GameTaxiPassengerSplineInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameTaxiPassengerSplineInsertCallbackId {
        GameTaxiPassengerSplineInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameTaxiPassengerSplineInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = GameTaxiPassengerSplineDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameTaxiPassengerSplineDeleteCallbackId {
        GameTaxiPassengerSplineDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameTaxiPassengerSplineDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

impl<'ctx> __sdk::WithInsert for GameTaxiPassengerSplineTableHandle<'ctx> {
    type InsertCallbackId = GameTaxiPassengerSplineInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameTaxiPassengerSplineInsertCallbackId {
        GameTaxiPassengerSplineInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameTaxiPassengerSplineInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }
}

impl<'ctx> __sdk::WithDelete for GameTaxiPassengerSplineTableHandle<'ctx> {
    type DeleteCallbackId = GameTaxiPassengerSplineDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameTaxiPassengerSplineDeleteCallbackId {
        GameTaxiPassengerSplineDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameTaxiPassengerSplineDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct GameTaxiPassengerSplineUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for GameTaxiPassengerSplineTableHandle<'ctx> {
    type UpdateCallbackId = GameTaxiPassengerSplineUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameTaxiPassengerSplineUpdateCallbackId {
        GameTaxiPassengerSplineUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameTaxiPassengerSplineUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

impl<'ctx> __sdk::WithUpdate for GameTaxiPassengerSplineTableHandle<'ctx> {
    type UpdateCallbackId = GameTaxiPassengerSplineUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameTaxiPassengerSplineUpdateCallbackId {
        GameTaxiPassengerSplineUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameTaxiPassengerSplineUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `character_guid` unique index on the table `game_taxi_passenger_spline`,
/// which allows point queries on the field of the same name
/// via the [`GameTaxiPassengerSplineCharacterGuidUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_taxi_passenger_spline().character_guid().find(...)`.
pub struct GameTaxiPassengerSplineCharacterGuidUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<TaxiPassengerSpline, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> GameTaxiPassengerSplineTableHandle<'ctx> {
    /// Get a handle on the `character_guid` unique index on the table `game_taxi_passenger_spline`.
    pub fn character_guid(&self) -> GameTaxiPassengerSplineCharacterGuidUnique<'ctx> {
        GameTaxiPassengerSplineCharacterGuidUnique {
            imp: self.imp.get_unique_constraint::<u64>("character_guid"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> GameTaxiPassengerSplineCharacterGuidUnique<'ctx> {
    /// Find the subscribed row whose `character_guid` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TaxiPassengerSpline> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table =
        client_cache.get_or_make_table::<TaxiPassengerSpline>("game_taxi_passenger_spline");
    _table.add_unique_constraint::<u64>("character_guid", |row| &row.character_guid);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<TaxiPassengerSpline>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<TaxiPassengerSpline>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `TaxiPassengerSpline`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait game_taxi_passenger_splineQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `TaxiPassengerSpline`.
    fn game_taxi_passenger_spline(&self) -> __sdk::__query_builder::Table<TaxiPassengerSpline>;
}

impl game_taxi_passenger_splineQueryTableAccess for __sdk::QueryTableAccessor {
    fn game_taxi_passenger_spline(&self) -> __sdk::__query_builder::Table<TaxiPassengerSpline> {
        __sdk::__query_builder::Table::new("game_taxi_passenger_spline")
    }
}
