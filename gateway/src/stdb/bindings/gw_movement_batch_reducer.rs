// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::gw_move_type::GwMove;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct GwMovementBatchArgs {
    pub moves: Vec<GwMove>,
}

impl From<GwMovementBatchArgs> for super::Reducer {
    fn from(args: GwMovementBatchArgs) -> Self {
        Self::GwMovementBatch { moves: args.moves }
    }
}

impl __sdk::InModule for GwMovementBatchArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `gw_movement_batch`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait gw_movement_batch {
    /// Request that the remote module invoke the reducer `gw_movement_batch` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`gw_movement_batch:gw_movement_batch_then`] to run a callback after the reducer completes.
    fn gw_movement_batch(&self, moves: Vec<GwMove>) -> __sdk::Result<()> {
        self.gw_movement_batch_then(moves, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `gw_movement_batch` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn gw_movement_batch_then(
        &self,
        moves: Vec<GwMove>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl gw_movement_batch for super::RemoteReducers {
    fn gw_movement_batch_then(
        &self,
        moves: Vec<GwMove>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(GwMovementBatchArgs { moves }, callback)
    }
}
