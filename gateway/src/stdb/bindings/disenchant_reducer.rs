// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DisenchantArgs {
    pub slot: u8,
}

impl From<DisenchantArgs> for super::Reducer {
    fn from(args: DisenchantArgs) -> Self {
        Self::Disenchant { slot: args.slot }
    }
}

impl __sdk::InModule for DisenchantArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `disenchant`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait disenchant {
    /// Request that the remote module invoke the reducer `disenchant` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`disenchant:disenchant_then`] to run a callback after the reducer completes.
    fn disenchant(&self, slot: u8) -> __sdk::Result<()> {
        self.disenchant_then(slot, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `disenchant` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn disenchant_then(
        &self,
        slot: u8,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl disenchant for super::RemoteReducers {
    fn disenchant_then(
        &self,
        slot: u8,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(DisenchantArgs { slot }, callback)
    }
}
