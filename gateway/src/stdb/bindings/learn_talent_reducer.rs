// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct LearnTalentArgs {
    pub talent_id: u32,
}

impl From<LearnTalentArgs> for super::Reducer {
    fn from(args: LearnTalentArgs) -> Self {
        Self::LearnTalent {
            talent_id: args.talent_id,
        }
    }
}

impl __sdk::InModule for LearnTalentArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `learn_talent`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait learn_talent {
    /// Request that the remote module invoke the reducer `learn_talent` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`learn_talent:learn_talent_then`] to run a callback after the reducer completes.
    fn learn_talent(&self, talent_id: u32) -> __sdk::Result<()> {
        self.learn_talent_then(talent_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `learn_talent` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn learn_talent_then(
        &self,
        talent_id: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl learn_talent for super::RemoteReducers {
    fn learn_talent_then(
        &self,
        talent_id: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(LearnTalentArgs { talent_id }, callback)
    }
}
