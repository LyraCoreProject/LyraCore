// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::threat_entry_type::ThreatEntry;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `game_threat`.
///
/// Obtain a handle from the [`GameThreatTableAccess::game_threat`] method on [`super::RemoteTables`],
/// like `ctx.db.game_threat()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_threat().on_insert(...)`.
pub struct GameThreatTableHandle<'ctx> {
    imp: __sdk::TableHandle<ThreatEntry>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `game_threat`.
///
/// Implemented for [`super::RemoteTables`].
pub trait GameThreatTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`GameThreatTableHandle`], which mediates access to the table `game_threat`.
    fn game_threat(&self) -> GameThreatTableHandle<'_>;
}

impl GameThreatTableAccess for super::RemoteTables {
    fn game_threat(&self) -> GameThreatTableHandle<'_> {
        GameThreatTableHandle {
            imp: self.imp.get_table::<ThreatEntry>("game_threat"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct GameThreatInsertCallbackId(__sdk::CallbackId);
pub struct GameThreatDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for GameThreatTableHandle<'ctx> {
    type Row = ThreatEntry;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = ThreatEntry> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = GameThreatInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameThreatInsertCallbackId {
        GameThreatInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameThreatInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = GameThreatDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameThreatDeleteCallbackId {
        GameThreatDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameThreatDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct GameThreatUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for GameThreatTableHandle<'ctx> {
    type UpdateCallbackId = GameThreatUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameThreatUpdateCallbackId {
        GameThreatUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameThreatUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `game_threat`,
/// which allows point queries on the field of the same name
/// via the [`GameThreatIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_threat().id().find(...)`.
pub struct GameThreatIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<ThreatEntry, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> GameThreatTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `game_threat`.
    pub fn id(&self) -> GameThreatIdUnique<'ctx> {
        GameThreatIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> GameThreatIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<ThreatEntry> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<ThreatEntry>("game_threat");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<ThreatEntry>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<ThreatEntry>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `ThreatEntry`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait game_threatQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `ThreatEntry`.
    fn game_threat(&self) -> __sdk::__query_builder::Table<ThreatEntry>;
}

impl game_threatQueryTableAccess for __sdk::QueryTableAccessor {
    fn game_threat(&self) -> __sdk::__query_builder::Table<ThreatEntry> {
        __sdk::__query_builder::Table::new("game_threat")
    }
}
