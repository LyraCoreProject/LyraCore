// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct GwMovementUpdateArgs {
    pub actor_guid: u64,
    pub opcode: u16,
    pub movement_info: Vec<u8>,
    pub x: f32,
    pub y: f32,
    pub z: f32,
    pub o: f32,
    pub move_time_ms: u32,
}

impl From<GwMovementUpdateArgs> for super::Reducer {
    fn from(args: GwMovementUpdateArgs) -> Self {
        Self::GwMovementUpdate {
            actor_guid: args.actor_guid,
            opcode: args.opcode,
            movement_info: args.movement_info,
            x: args.x,
            y: args.y,
            z: args.z,
            o: args.o,
            move_time_ms: args.move_time_ms,
        }
    }
}

impl __sdk::InModule for GwMovementUpdateArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `gw_movement_update`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait gw_movement_update {
    /// Request that the remote module invoke the reducer `gw_movement_update` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`gw_movement_update:gw_movement_update_then`] to run a callback after the reducer completes.
    fn gw_movement_update(
        &self,
        actor_guid: u64,
        opcode: u16,
        movement_info: Vec<u8>,
        x: f32,
        y: f32,
        z: f32,
        o: f32,
        move_time_ms: u32,
    ) -> __sdk::Result<()> {
        self.gw_movement_update_then(
            actor_guid,
            opcode,
            movement_info,
            x,
            y,
            z,
            o,
            move_time_ms,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `gw_movement_update` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn gw_movement_update_then(
        &self,
        actor_guid: u64,
        opcode: u16,
        movement_info: Vec<u8>,
        x: f32,
        y: f32,
        z: f32,
        o: f32,
        move_time_ms: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl gw_movement_update for super::RemoteReducers {
    fn gw_movement_update_then(
        &self,
        actor_guid: u64,
        opcode: u16,
        movement_info: Vec<u8>,
        x: f32,
        y: f32,
        z: f32,
        o: f32,
        move_time_ms: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            GwMovementUpdateArgs {
                actor_guid,
                opcode,
                movement_info,
                x,
                y,
                z,
                o,
                move_time_ms,
            },
            callback,
        )
    }
}
