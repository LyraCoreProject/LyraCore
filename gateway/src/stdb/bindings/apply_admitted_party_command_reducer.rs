// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ApplyAdmittedPartyCommandArgs {
    pub source_identity: __sdk::Identity,
    pub intent_id: u64,
    pub issuer_guid: u64,
    pub group_id: u64,
    pub leader_guid: u64,
    pub members: Vec<u64>,
    pub kind: u8,
    pub bot_guid: u64,
    pub authority_member_guid: u64,
    pub exact_target_guid: u64,
    pub expires_micros: i64,
    pub receipt_retain_until_micros: i64,
}

impl From<ApplyAdmittedPartyCommandArgs> for super::Reducer {
    fn from(args: ApplyAdmittedPartyCommandArgs) -> Self {
        Self::ApplyAdmittedPartyCommand {
            source_identity: args.source_identity,
            intent_id: args.intent_id,
            issuer_guid: args.issuer_guid,
            group_id: args.group_id,
            leader_guid: args.leader_guid,
            members: args.members,
            kind: args.kind,
            bot_guid: args.bot_guid,
            authority_member_guid: args.authority_member_guid,
            exact_target_guid: args.exact_target_guid,
            expires_micros: args.expires_micros,
            receipt_retain_until_micros: args.receipt_retain_until_micros,
        }
    }
}

impl __sdk::InModule for ApplyAdmittedPartyCommandArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `apply_admitted_party_command`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait apply_admitted_party_command {
    /// Request that the remote module invoke the reducer `apply_admitted_party_command` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`apply_admitted_party_command:apply_admitted_party_command_then`] to run a callback after the reducer completes.
    fn apply_admitted_party_command(
        &self,
        source_identity: __sdk::Identity,
        intent_id: u64,
        issuer_guid: u64,
        group_id: u64,
        leader_guid: u64,
        members: Vec<u64>,
        kind: u8,
        bot_guid: u64,
        authority_member_guid: u64,
        exact_target_guid: u64,
        expires_micros: i64,
        receipt_retain_until_micros: i64,
    ) -> __sdk::Result<()> {
        self.apply_admitted_party_command_then(
            source_identity,
            intent_id,
            issuer_guid,
            group_id,
            leader_guid,
            members,
            kind,
            bot_guid,
            authority_member_guid,
            exact_target_guid,
            expires_micros,
            receipt_retain_until_micros,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `apply_admitted_party_command` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn apply_admitted_party_command_then(
        &self,
        source_identity: __sdk::Identity,
        intent_id: u64,
        issuer_guid: u64,
        group_id: u64,
        leader_guid: u64,
        members: Vec<u64>,
        kind: u8,
        bot_guid: u64,
        authority_member_guid: u64,
        exact_target_guid: u64,
        expires_micros: i64,
        receipt_retain_until_micros: i64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl apply_admitted_party_command for super::RemoteReducers {
    fn apply_admitted_party_command_then(
        &self,
        source_identity: __sdk::Identity,
        intent_id: u64,
        issuer_guid: u64,
        group_id: u64,
        leader_guid: u64,
        members: Vec<u64>,
        kind: u8,
        bot_guid: u64,
        authority_member_guid: u64,
        exact_target_guid: u64,
        expires_micros: i64,
        receipt_retain_until_micros: i64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ApplyAdmittedPartyCommandArgs {
                source_identity,
                intent_id,
                issuer_guid,
                group_id,
                leader_guid,
                members,
                kind,
                bot_guid,
                authority_member_guid,
                exact_target_guid,
                expires_micros,
                receipt_retain_until_micros,
            },
            callback,
        )
    }
}
