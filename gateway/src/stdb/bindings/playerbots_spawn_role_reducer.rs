// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct PlayerbotsSpawnRoleArgs {
    pub count: u32,
    pub x: f32,
    pub y: f32,
    pub z: f32,
    pub role: u8,
}

impl From<PlayerbotsSpawnRoleArgs> for super::Reducer {
    fn from(args: PlayerbotsSpawnRoleArgs) -> Self {
        Self::PlayerbotsSpawnRole {
            count: args.count,
            x: args.x,
            y: args.y,
            z: args.z,
            role: args.role,
        }
    }
}

impl __sdk::InModule for PlayerbotsSpawnRoleArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `playerbots_spawn_role`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait playerbots_spawn_role {
    /// Request that the remote module invoke the reducer `playerbots_spawn_role` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`playerbots_spawn_role:playerbots_spawn_role_then`] to run a callback after the reducer completes.
    fn playerbots_spawn_role(
        &self,
        count: u32,
        x: f32,
        y: f32,
        z: f32,
        role: u8,
    ) -> __sdk::Result<()> {
        self.playerbots_spawn_role_then(count, x, y, z, role, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `playerbots_spawn_role` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn playerbots_spawn_role_then(
        &self,
        count: u32,
        x: f32,
        y: f32,
        z: f32,
        role: u8,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl playerbots_spawn_role for super::RemoteReducers {
    fn playerbots_spawn_role_then(
        &self,
        count: u32,
        x: f32,
        y: f32,
        z: f32,
        role: u8,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            PlayerbotsSpawnRoleArgs {
                count,
                x,
                y,
                z,
                role,
            },
            callback,
        )
    }
}
