// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::guild_type::Guild;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `game_guild`.
///
/// Obtain a handle from the [`GameGuildTableAccess::game_guild`] method on [`super::RemoteTables`],
/// like `ctx.db.game_guild()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_guild().on_insert(...)`.
pub struct GameGuildTableHandle<'ctx> {
    imp: __sdk::TableHandle<Guild>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

/// Lifetime-aware accessor marker for the table `game_guild`.
pub struct GameGuildTableAccessor;

impl __sdk::TableAccessor<super::RemoteTables> for GameGuildTableAccessor {
    type Row = Guild;
    type Handle<'db> = GameGuildTableHandle<'db>;

    fn get<'db>(db: &'db super::RemoteTables) -> Self::Handle<'db> {
        db.game_guild()
    }
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `game_guild`.
///
/// Implemented for [`super::RemoteTables`].
pub trait GameGuildTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`GameGuildTableHandle`], which mediates access to the table `game_guild`.
    fn game_guild(&self) -> GameGuildTableHandle<'_>;
}

impl GameGuildTableAccess for super::RemoteTables {
    fn game_guild(&self) -> GameGuildTableHandle<'_> {
        GameGuildTableHandle {
            imp: self.imp.get_table::<Guild>("game_guild"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct GameGuildInsertCallbackId(__sdk::CallbackId);
pub struct GameGuildDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableLike for GameGuildTableHandle<'ctx> {
    type Row = Guild;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = Guild> + '_ {
        self.imp.iter()
    }
}

impl<'ctx> __sdk::Table for GameGuildTableHandle<'ctx> {
    type Row = Guild;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = Guild> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = GameGuildInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameGuildInsertCallbackId {
        GameGuildInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameGuildInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = GameGuildDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameGuildDeleteCallbackId {
        GameGuildDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameGuildDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

impl<'ctx> __sdk::WithInsert for GameGuildTableHandle<'ctx> {
    type InsertCallbackId = GameGuildInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameGuildInsertCallbackId {
        GameGuildInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameGuildInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }
}

impl<'ctx> __sdk::WithDelete for GameGuildTableHandle<'ctx> {
    type DeleteCallbackId = GameGuildDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameGuildDeleteCallbackId {
        GameGuildDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameGuildDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct GameGuildUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for GameGuildTableHandle<'ctx> {
    type UpdateCallbackId = GameGuildUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameGuildUpdateCallbackId {
        GameGuildUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameGuildUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

impl<'ctx> __sdk::WithUpdate for GameGuildTableHandle<'ctx> {
    type UpdateCallbackId = GameGuildUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameGuildUpdateCallbackId {
        GameGuildUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameGuildUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `guild_id` unique index on the table `game_guild`,
/// which allows point queries on the field of the same name
/// via the [`GameGuildGuildIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_guild().guild_id().find(...)`.
pub struct GameGuildGuildIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<Guild, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> GameGuildTableHandle<'ctx> {
    /// Get a handle on the `guild_id` unique index on the table `game_guild`.
    pub fn guild_id(&self) -> GameGuildGuildIdUnique<'ctx> {
        GameGuildGuildIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("guild_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> GameGuildGuildIdUnique<'ctx> {
    /// Find the subscribed row whose `guild_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<Guild> {
        self.imp.find(col_val)
    }
}

/// Access to the `name` unique index on the table `game_guild`,
/// which allows point queries on the field of the same name
/// via the [`GameGuildNameUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_guild().name().find(...)`.
pub struct GameGuildNameUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<Guild, String>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> GameGuildTableHandle<'ctx> {
    /// Get a handle on the `name` unique index on the table `game_guild`.
    pub fn name(&self) -> GameGuildNameUnique<'ctx> {
        GameGuildNameUnique {
            imp: self.imp.get_unique_constraint::<String>("name"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> GameGuildNameUnique<'ctx> {
    /// Find the subscribed row whose `name` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &String) -> Option<Guild> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<Guild>("game_guild");
    _table.add_unique_constraint::<u64>("guild_id", |row| &row.guild_id);
    _table.add_unique_constraint::<String>("name", |row| &row.name);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<Guild>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<Guild>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `Guild`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait game_guildQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `Guild`.
    fn game_guild(&self) -> __sdk::__query_builder::Table<Guild>;
}

impl game_guildQueryTableAccess for __sdk::QueryTableAccessor {
    fn game_guild(&self) -> __sdk::__query_builder::Table<Guild> {
        __sdk::__query_builder::Table::new("game_guild")
    }
}
