// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct BuybackItemArgs {
    pub vendor_guid: u64,
    pub slot: u8,
}

impl From<BuybackItemArgs> for super::Reducer {
    fn from(args: BuybackItemArgs) -> Self {
        Self::BuybackItem {
            vendor_guid: args.vendor_guid,
            slot: args.slot,
        }
    }
}

impl __sdk::InModule for BuybackItemArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `buyback_item`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait buyback_item {
    /// Request that the remote module invoke the reducer `buyback_item` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`buyback_item:buyback_item_then`] to run a callback after the reducer completes.
    fn buyback_item(&self, vendor_guid: u64, slot: u8) -> __sdk::Result<()> {
        self.buyback_item_then(vendor_guid, slot, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `buyback_item` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn buyback_item_then(
        &self,
        vendor_guid: u64,
        slot: u8,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl buyback_item for super::RemoteReducers {
    fn buyback_item_then(
        &self,
        vendor_guid: u64,
        slot: u8,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(BuybackItemArgs { vendor_guid, slot }, callback)
    }
}
