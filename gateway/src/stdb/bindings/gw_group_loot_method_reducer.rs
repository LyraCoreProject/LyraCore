// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct GwGroupLootMethodArgs {
    pub actor_guid: u64,
    pub loot_setting: u8,
    pub master_guid: u64,
    pub loot_threshold: u8,
}

impl From<GwGroupLootMethodArgs> for super::Reducer {
    fn from(args: GwGroupLootMethodArgs) -> Self {
        Self::GwGroupLootMethod {
            actor_guid: args.actor_guid,
            loot_setting: args.loot_setting,
            master_guid: args.master_guid,
            loot_threshold: args.loot_threshold,
        }
    }
}

impl __sdk::InModule for GwGroupLootMethodArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `gw_group_loot_method`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait gw_group_loot_method {
    /// Request that the remote module invoke the reducer `gw_group_loot_method` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`gw_group_loot_method:gw_group_loot_method_then`] to run a callback after the reducer completes.
    fn gw_group_loot_method(
        &self,
        actor_guid: u64,
        loot_setting: u8,
        master_guid: u64,
        loot_threshold: u8,
    ) -> __sdk::Result<()> {
        self.gw_group_loot_method_then(
            actor_guid,
            loot_setting,
            master_guid,
            loot_threshold,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `gw_group_loot_method` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn gw_group_loot_method_then(
        &self,
        actor_guid: u64,
        loot_setting: u8,
        master_guid: u64,
        loot_threshold: u8,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl gw_group_loot_method for super::RemoteReducers {
    fn gw_group_loot_method_then(
        &self,
        actor_guid: u64,
        loot_setting: u8,
        master_guid: u64,
        loot_threshold: u8,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            GwGroupLootMethodArgs {
                actor_guid,
                loot_setting,
                master_guid,
                loot_threshold,
            },
            callback,
        )
    }
}
