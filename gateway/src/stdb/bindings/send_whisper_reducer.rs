// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SendWhisperArgs {
    pub target_name: String,
    pub message: String,
}

impl From<SendWhisperArgs> for super::Reducer {
    fn from(args: SendWhisperArgs) -> Self {
        Self::SendWhisper {
            target_name: args.target_name,
            message: args.message,
        }
    }
}

impl __sdk::InModule for SendWhisperArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `send_whisper`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait send_whisper {
    /// Request that the remote module invoke the reducer `send_whisper` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`send_whisper:send_whisper_then`] to run a callback after the reducer completes.
    fn send_whisper(&self, target_name: String, message: String) -> __sdk::Result<()> {
        self.send_whisper_then(target_name, message, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `send_whisper` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn send_whisper_then(
        &self,
        target_name: String,
        message: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl send_whisper for super::RemoteReducers {
    fn send_whisper_then(
        &self,
        target_name: String,
        message: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SendWhisperArgs {
                target_name,
                message,
            },
            callback,
        )
    }
}
