// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::event_reaper_schedule_type::EventReaperSchedule;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ReapMovementEventsArgs {
    pub schedule: EventReaperSchedule,
}

impl From<ReapMovementEventsArgs> for super::Reducer {
    fn from(args: ReapMovementEventsArgs) -> Self {
        Self::ReapMovementEvents {
            schedule: args.schedule,
        }
    }
}

impl __sdk::InModule for ReapMovementEventsArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `reap_movement_events`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait reap_movement_events {
    /// Request that the remote module invoke the reducer `reap_movement_events` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`reap_movement_events:reap_movement_events_then`] to run a callback after the reducer completes.
    fn reap_movement_events(&self, schedule: EventReaperSchedule) -> __sdk::Result<()> {
        self.reap_movement_events_then(schedule, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `reap_movement_events` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn reap_movement_events_then(
        &self,
        schedule: EventReaperSchedule,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl reap_movement_events for super::RemoteReducers {
    fn reap_movement_events_then(
        &self,
        schedule: EventReaperSchedule,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(ReapMovementEventsArgs { schedule }, callback)
    }
}
