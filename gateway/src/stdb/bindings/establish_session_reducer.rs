// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct EstablishSessionArgs {
    pub account_id: u64,
    pub session_key: Vec<u8>,
    pub bound_identity: __sdk::Identity,
}

impl From<EstablishSessionArgs> for super::Reducer {
    fn from(args: EstablishSessionArgs) -> Self {
        Self::EstablishSession {
            account_id: args.account_id,
            session_key: args.session_key,
            bound_identity: args.bound_identity,
        }
    }
}

impl __sdk::InModule for EstablishSessionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `establish_session`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait establish_session {
    /// Request that the remote module invoke the reducer `establish_session` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`establish_session:establish_session_then`] to run a callback after the reducer completes.
    fn establish_session(
        &self,
        account_id: u64,
        session_key: Vec<u8>,
        bound_identity: __sdk::Identity,
    ) -> __sdk::Result<()> {
        self.establish_session_then(account_id, session_key, bound_identity, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `establish_session` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn establish_session_then(
        &self,
        account_id: u64,
        session_key: Vec<u8>,
        bound_identity: __sdk::Identity,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl establish_session for super::RemoteReducers {
    fn establish_session_then(
        &self,
        account_id: u64,
        session_key: Vec<u8>,
        bound_identity: __sdk::Identity,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            EstablishSessionArgs {
                account_id,
                session_key,
                bound_identity,
            },
            callback,
        )
    }
}
