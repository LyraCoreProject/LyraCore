// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::buyback_entry_type::BuybackEntry;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `game_character_buyback`.
///
/// Obtain a handle from the [`GameCharacterBuybackTableAccess::game_character_buyback`] method on [`super::RemoteTables`],
/// like `ctx.db.game_character_buyback()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_character_buyback().on_insert(...)`.
pub struct GameCharacterBuybackTableHandle<'ctx> {
    imp: __sdk::TableHandle<BuybackEntry>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `game_character_buyback`.
///
/// Implemented for [`super::RemoteTables`].
pub trait GameCharacterBuybackTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`GameCharacterBuybackTableHandle`], which mediates access to the table `game_character_buyback`.
    fn game_character_buyback(&self) -> GameCharacterBuybackTableHandle<'_>;
}

impl GameCharacterBuybackTableAccess for super::RemoteTables {
    fn game_character_buyback(&self) -> GameCharacterBuybackTableHandle<'_> {
        GameCharacterBuybackTableHandle {
            imp: self.imp.get_table::<BuybackEntry>("game_character_buyback"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct GameCharacterBuybackInsertCallbackId(__sdk::CallbackId);
pub struct GameCharacterBuybackDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for GameCharacterBuybackTableHandle<'ctx> {
    type Row = BuybackEntry;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = BuybackEntry> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = GameCharacterBuybackInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameCharacterBuybackInsertCallbackId {
        GameCharacterBuybackInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameCharacterBuybackInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = GameCharacterBuybackDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameCharacterBuybackDeleteCallbackId {
        GameCharacterBuybackDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameCharacterBuybackDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct GameCharacterBuybackUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for GameCharacterBuybackTableHandle<'ctx> {
    type UpdateCallbackId = GameCharacterBuybackUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameCharacterBuybackUpdateCallbackId {
        GameCharacterBuybackUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameCharacterBuybackUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `game_character_buyback`,
/// which allows point queries on the field of the same name
/// via the [`GameCharacterBuybackIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_character_buyback().id().find(...)`.
pub struct GameCharacterBuybackIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<BuybackEntry, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> GameCharacterBuybackTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `game_character_buyback`.
    pub fn id(&self) -> GameCharacterBuybackIdUnique<'ctx> {
        GameCharacterBuybackIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> GameCharacterBuybackIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<BuybackEntry> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<BuybackEntry>("game_character_buyback");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<BuybackEntry>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<BuybackEntry>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `BuybackEntry`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait game_character_buybackQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `BuybackEntry`.
    fn game_character_buyback(&self) -> __sdk::__query_builder::Table<BuybackEntry>;
}

impl game_character_buybackQueryTableAccess for __sdk::QueryTableAccessor {
    fn game_character_buyback(&self) -> __sdk::__query_builder::Table<BuybackEntry> {
        __sdk::__query_builder::Table::new("game_character_buyback")
    }
}
