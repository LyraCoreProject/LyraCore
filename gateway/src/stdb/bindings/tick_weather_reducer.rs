// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::weather_schedule_type::WeatherSchedule;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct TickWeatherArgs {
    pub schedule: WeatherSchedule,
}

impl From<TickWeatherArgs> for super::Reducer {
    fn from(args: TickWeatherArgs) -> Self {
        Self::TickWeather {
            schedule: args.schedule,
        }
    }
}

impl __sdk::InModule for TickWeatherArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `tick_weather`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait tick_weather {
    /// Request that the remote module invoke the reducer `tick_weather` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`tick_weather:tick_weather_then`] to run a callback after the reducer completes.
    fn tick_weather(&self, schedule: WeatherSchedule) -> __sdk::Result<()> {
        self.tick_weather_then(schedule, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `tick_weather` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn tick_weather_then(
        &self,
        schedule: WeatherSchedule,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl tick_weather for super::RemoteReducers {
    fn tick_weather_then(
        &self,
        schedule: WeatherSchedule,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(TickWeatherArgs { schedule }, callback)
    }
}
