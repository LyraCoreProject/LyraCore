// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::ground_area_type::GroundArea;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `game_ground_area`.
///
/// Obtain a handle from the [`GameGroundAreaTableAccess::game_ground_area`] method on [`super::RemoteTables`],
/// like `ctx.db.game_ground_area()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_ground_area().on_insert(...)`.
pub struct GameGroundAreaTableHandle<'ctx> {
    imp: __sdk::TableHandle<GroundArea>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

/// Lifetime-aware accessor marker for the table `game_ground_area`.
pub struct GameGroundAreaTableAccessor;

impl __sdk::TableAccessor<super::RemoteTables> for GameGroundAreaTableAccessor {
    type Row = GroundArea;
    type Handle<'db> = GameGroundAreaTableHandle<'db>;

    fn get<'db>(db: &'db super::RemoteTables) -> Self::Handle<'db> {
        db.game_ground_area()
    }
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `game_ground_area`.
///
/// Implemented for [`super::RemoteTables`].
pub trait GameGroundAreaTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`GameGroundAreaTableHandle`], which mediates access to the table `game_ground_area`.
    fn game_ground_area(&self) -> GameGroundAreaTableHandle<'_>;
}

impl GameGroundAreaTableAccess for super::RemoteTables {
    fn game_ground_area(&self) -> GameGroundAreaTableHandle<'_> {
        GameGroundAreaTableHandle {
            imp: self.imp.get_table::<GroundArea>("game_ground_area"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct GameGroundAreaInsertCallbackId(__sdk::CallbackId);
pub struct GameGroundAreaDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableLike for GameGroundAreaTableHandle<'ctx> {
    type Row = GroundArea;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = GroundArea> + '_ {
        self.imp.iter()
    }
}

impl<'ctx> __sdk::Table for GameGroundAreaTableHandle<'ctx> {
    type Row = GroundArea;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = GroundArea> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = GameGroundAreaInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameGroundAreaInsertCallbackId {
        GameGroundAreaInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameGroundAreaInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = GameGroundAreaDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameGroundAreaDeleteCallbackId {
        GameGroundAreaDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameGroundAreaDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

impl<'ctx> __sdk::WithInsert for GameGroundAreaTableHandle<'ctx> {
    type InsertCallbackId = GameGroundAreaInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameGroundAreaInsertCallbackId {
        GameGroundAreaInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameGroundAreaInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }
}

impl<'ctx> __sdk::WithDelete for GameGroundAreaTableHandle<'ctx> {
    type DeleteCallbackId = GameGroundAreaDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameGroundAreaDeleteCallbackId {
        GameGroundAreaDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameGroundAreaDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct GameGroundAreaUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for GameGroundAreaTableHandle<'ctx> {
    type UpdateCallbackId = GameGroundAreaUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameGroundAreaUpdateCallbackId {
        GameGroundAreaUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameGroundAreaUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

impl<'ctx> __sdk::WithUpdate for GameGroundAreaTableHandle<'ctx> {
    type UpdateCallbackId = GameGroundAreaUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameGroundAreaUpdateCallbackId {
        GameGroundAreaUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameGroundAreaUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `game_ground_area`,
/// which allows point queries on the field of the same name
/// via the [`GameGroundAreaIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_ground_area().id().find(...)`.
pub struct GameGroundAreaIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<GroundArea, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> GameGroundAreaTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `game_ground_area`.
    pub fn id(&self) -> GameGroundAreaIdUnique<'ctx> {
        GameGroundAreaIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> GameGroundAreaIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<GroundArea> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<GroundArea>("game_ground_area");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<GroundArea>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<GroundArea>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `GroundArea`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait game_ground_areaQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `GroundArea`.
    fn game_ground_area(&self) -> __sdk::__query_builder::Table<GroundArea>;
}

impl game_ground_areaQueryTableAccess for __sdk::QueryTableAccessor {
    fn game_ground_area(&self) -> __sdk::__query_builder::Table<GroundArea> {
        __sdk::__query_builder::Table::new("game_ground_area")
    }
}
