// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::auction_expiry_type::AuctionExpiry;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ExpireAuctionArgs {
    pub schedule: AuctionExpiry,
}

impl From<ExpireAuctionArgs> for super::Reducer {
    fn from(args: ExpireAuctionArgs) -> Self {
        Self::ExpireAuction {
            schedule: args.schedule,
        }
    }
}

impl __sdk::InModule for ExpireAuctionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `expire_auction`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait expire_auction {
    /// Request that the remote module invoke the reducer `expire_auction` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`expire_auction:expire_auction_then`] to run a callback after the reducer completes.
    fn expire_auction(&self, schedule: AuctionExpiry) -> __sdk::Result<()> {
        self.expire_auction_then(schedule, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `expire_auction` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn expire_auction_then(
        &self,
        schedule: AuctionExpiry,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl expire_auction for super::RemoteReducers {
    fn expire_auction_then(
        &self,
        schedule: AuctionExpiry,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(ExpireAuctionArgs { schedule }, callback)
    }
}
