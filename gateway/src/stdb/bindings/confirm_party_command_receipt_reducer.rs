// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ConfirmPartyCommandReceiptArgs {
    pub source_identity: __sdk::Identity,
    pub intent_id: u64,
}

impl From<ConfirmPartyCommandReceiptArgs> for super::Reducer {
    fn from(args: ConfirmPartyCommandReceiptArgs) -> Self {
        Self::ConfirmPartyCommandReceipt {
            source_identity: args.source_identity,
            intent_id: args.intent_id,
        }
    }
}

impl __sdk::InModule for ConfirmPartyCommandReceiptArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `confirm_party_command_receipt`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait confirm_party_command_receipt {
    /// Request that the remote module invoke the reducer `confirm_party_command_receipt` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`confirm_party_command_receipt:confirm_party_command_receipt_then`] to run a callback after the reducer completes.
    fn confirm_party_command_receipt(
        &self,
        source_identity: __sdk::Identity,
        intent_id: u64,
    ) -> __sdk::Result<()> {
        self.confirm_party_command_receipt_then(source_identity, intent_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `confirm_party_command_receipt` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn confirm_party_command_receipt_then(
        &self,
        source_identity: __sdk::Identity,
        intent_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl confirm_party_command_receipt for super::RemoteReducers {
    fn confirm_party_command_receipt_then(
        &self,
        source_identity: __sdk::Identity,
        intent_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ConfirmPartyCommandReceiptArgs {
                source_identity,
                intent_id,
            },
            callback,
        )
    }
}
