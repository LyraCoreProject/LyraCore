// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::contact_entry_type::ContactEntry;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `game_character_contact`.
///
/// Obtain a handle from the [`GameCharacterContactTableAccess::game_character_contact`] method on [`super::RemoteTables`],
/// like `ctx.db.game_character_contact()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_character_contact().on_insert(...)`.
pub struct GameCharacterContactTableHandle<'ctx> {
    imp: __sdk::TableHandle<ContactEntry>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `game_character_contact`.
///
/// Implemented for [`super::RemoteTables`].
pub trait GameCharacterContactTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`GameCharacterContactTableHandle`], which mediates access to the table `game_character_contact`.
    fn game_character_contact(&self) -> GameCharacterContactTableHandle<'_>;
}

impl GameCharacterContactTableAccess for super::RemoteTables {
    fn game_character_contact(&self) -> GameCharacterContactTableHandle<'_> {
        GameCharacterContactTableHandle {
            imp: self.imp.get_table::<ContactEntry>("game_character_contact"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct GameCharacterContactInsertCallbackId(__sdk::CallbackId);
pub struct GameCharacterContactDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for GameCharacterContactTableHandle<'ctx> {
    type Row = ContactEntry;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = ContactEntry> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = GameCharacterContactInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameCharacterContactInsertCallbackId {
        GameCharacterContactInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameCharacterContactInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = GameCharacterContactDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameCharacterContactDeleteCallbackId {
        GameCharacterContactDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameCharacterContactDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct GameCharacterContactUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for GameCharacterContactTableHandle<'ctx> {
    type UpdateCallbackId = GameCharacterContactUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameCharacterContactUpdateCallbackId {
        GameCharacterContactUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameCharacterContactUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `game_character_contact`,
/// which allows point queries on the field of the same name
/// via the [`GameCharacterContactIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_character_contact().id().find(...)`.
pub struct GameCharacterContactIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<ContactEntry, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> GameCharacterContactTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `game_character_contact`.
    pub fn id(&self) -> GameCharacterContactIdUnique<'ctx> {
        GameCharacterContactIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> GameCharacterContactIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<ContactEntry> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<ContactEntry>("game_character_contact");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<ContactEntry>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<ContactEntry>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `ContactEntry`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait game_character_contactQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `ContactEntry`.
    fn game_character_contact(&self) -> __sdk::__query_builder::Table<ContactEntry>;
}

impl game_character_contactQueryTableAccess for __sdk::QueryTableAccessor {
    fn game_character_contact(&self) -> __sdk::__query_builder::Table<ContactEntry> {
        __sdk::__query_builder::Table::new("game_character_contact")
    }
}
