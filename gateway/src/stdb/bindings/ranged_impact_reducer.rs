// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::ranged_impact_schedule_type::RangedImpactSchedule;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RangedImpactArgs {
    pub shot: RangedImpactSchedule,
}

impl From<RangedImpactArgs> for super::Reducer {
    fn from(args: RangedImpactArgs) -> Self {
        Self::RangedImpact { shot: args.shot }
    }
}

impl __sdk::InModule for RangedImpactArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `ranged_impact`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait ranged_impact {
    /// Request that the remote module invoke the reducer `ranged_impact` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`ranged_impact:ranged_impact_then`] to run a callback after the reducer completes.
    fn ranged_impact(&self, shot: RangedImpactSchedule) -> __sdk::Result<()> {
        self.ranged_impact_then(shot, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `ranged_impact` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn ranged_impact_then(
        &self,
        shot: RangedImpactSchedule,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl ranged_impact for super::RemoteReducers {
    fn ranged_impact_then(
        &self,
        shot: RangedImpactSchedule,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(RangedImpactArgs { shot }, callback)
    }
}
