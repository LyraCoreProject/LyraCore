// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::session_actor_type::SessionActor;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ReleasePlayerTransferArrivalArgs {
    pub transfer_id: u64,
    pub character_guid: u64,
    pub source_map_id: u32,
    pub source_instance_id: u64,
    pub source_locator_revision: u64,
    pub request_actor: SessionActor,
}

impl From<ReleasePlayerTransferArrivalArgs> for super::Reducer {
    fn from(args: ReleasePlayerTransferArrivalArgs) -> Self {
        Self::ReleasePlayerTransferArrival {
            transfer_id: args.transfer_id,
            character_guid: args.character_guid,
            source_map_id: args.source_map_id,
            source_instance_id: args.source_instance_id,
            source_locator_revision: args.source_locator_revision,
            request_actor: args.request_actor,
        }
    }
}

impl __sdk::InModule for ReleasePlayerTransferArrivalArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `release_player_transfer_arrival`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait release_player_transfer_arrival {
    /// Request that the remote module invoke the reducer `release_player_transfer_arrival` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`release_player_transfer_arrival:release_player_transfer_arrival_then`] to run a callback after the reducer completes.
    fn release_player_transfer_arrival(
        &self,
        transfer_id: u64,
        character_guid: u64,
        source_map_id: u32,
        source_instance_id: u64,
        source_locator_revision: u64,
        request_actor: SessionActor,
    ) -> __sdk::Result<()> {
        self.release_player_transfer_arrival_then(
            transfer_id,
            character_guid,
            source_map_id,
            source_instance_id,
            source_locator_revision,
            request_actor,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `release_player_transfer_arrival` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn release_player_transfer_arrival_then(
        &self,
        transfer_id: u64,
        character_guid: u64,
        source_map_id: u32,
        source_instance_id: u64,
        source_locator_revision: u64,
        request_actor: SessionActor,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl release_player_transfer_arrival for super::RemoteReducers {
    fn release_player_transfer_arrival_then(
        &self,
        transfer_id: u64,
        character_guid: u64,
        source_map_id: u32,
        source_instance_id: u64,
        source_locator_revision: u64,
        request_actor: SessionActor,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ReleasePlayerTransferArrivalArgs {
                transfer_id,
                character_guid,
                source_map_id,
                source_instance_id,
                source_locator_revision,
                request_actor,
            },
            callback,
        )
    }
}
