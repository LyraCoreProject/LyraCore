// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SyncGroupMirrorArgs {
    pub group_id: u64,
    pub leader_guid: u64,
    pub loot_method_setting: u8,
    pub loot_threshold: u8,
    pub master_looter_guid: u64,
    pub members: Vec<u64>,
}

impl From<SyncGroupMirrorArgs> for super::Reducer {
    fn from(args: SyncGroupMirrorArgs) -> Self {
        Self::SyncGroupMirror {
            group_id: args.group_id,
            leader_guid: args.leader_guid,
            loot_method_setting: args.loot_method_setting,
            loot_threshold: args.loot_threshold,
            master_looter_guid: args.master_looter_guid,
            members: args.members,
        }
    }
}

impl __sdk::InModule for SyncGroupMirrorArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `sync_group_mirror`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait sync_group_mirror {
    /// Request that the remote module invoke the reducer `sync_group_mirror` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`sync_group_mirror:sync_group_mirror_then`] to run a callback after the reducer completes.
    fn sync_group_mirror(
        &self,
        group_id: u64,
        leader_guid: u64,
        loot_method_setting: u8,
        loot_threshold: u8,
        master_looter_guid: u64,
        members: Vec<u64>,
    ) -> __sdk::Result<()> {
        self.sync_group_mirror_then(
            group_id,
            leader_guid,
            loot_method_setting,
            loot_threshold,
            master_looter_guid,
            members,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `sync_group_mirror` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn sync_group_mirror_then(
        &self,
        group_id: u64,
        leader_guid: u64,
        loot_method_setting: u8,
        loot_threshold: u8,
        master_looter_guid: u64,
        members: Vec<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl sync_group_mirror for super::RemoteReducers {
    fn sync_group_mirror_then(
        &self,
        group_id: u64,
        leader_guid: u64,
        loot_method_setting: u8,
        loot_threshold: u8,
        master_looter_guid: u64,
        members: Vec<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SyncGroupMirrorArgs {
                group_id,
                leader_guid,
                loot_method_setting,
                loot_threshold,
                master_looter_guid,
                members,
            },
            callback,
        )
    }
}
