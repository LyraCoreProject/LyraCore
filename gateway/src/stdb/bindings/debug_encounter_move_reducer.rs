// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DebugEncounterMoveArgs {
    pub creature_guid: u64,
    pub x: f32,
    pub y: f32,
    pub z: f32,
    pub run: bool,
}

impl From<DebugEncounterMoveArgs> for super::Reducer {
    fn from(args: DebugEncounterMoveArgs) -> Self {
        Self::DebugEncounterMove {
            creature_guid: args.creature_guid,
            x: args.x,
            y: args.y,
            z: args.z,
            run: args.run,
        }
    }
}

impl __sdk::InModule for DebugEncounterMoveArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `debug_encounter_move`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait debug_encounter_move {
    /// Request that the remote module invoke the reducer `debug_encounter_move` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`debug_encounter_move:debug_encounter_move_then`] to run a callback after the reducer completes.
    fn debug_encounter_move(
        &self,
        creature_guid: u64,
        x: f32,
        y: f32,
        z: f32,
        run: bool,
    ) -> __sdk::Result<()> {
        self.debug_encounter_move_then(creature_guid, x, y, z, run, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `debug_encounter_move` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn debug_encounter_move_then(
        &self,
        creature_guid: u64,
        x: f32,
        y: f32,
        z: f32,
        run: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl debug_encounter_move for super::RemoteReducers {
    fn debug_encounter_move_then(
        &self,
        creature_guid: u64,
        x: f32,
        y: f32,
        z: f32,
        run: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DebugEncounterMoveArgs {
                creature_guid,
                x,
                y,
                z,
                run,
            },
            callback,
        )
    }
}
