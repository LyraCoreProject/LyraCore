// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct EnchantItemArgs {
    pub target_slot: u8,
    pub enchant_id: u32,
}

impl From<EnchantItemArgs> for super::Reducer {
    fn from(args: EnchantItemArgs) -> Self {
        Self::EnchantItem {
            target_slot: args.target_slot,
            enchant_id: args.enchant_id,
        }
    }
}

impl __sdk::InModule for EnchantItemArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `enchant_item`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait enchant_item {
    /// Request that the remote module invoke the reducer `enchant_item` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`enchant_item:enchant_item_then`] to run a callback after the reducer completes.
    fn enchant_item(&self, target_slot: u8, enchant_id: u32) -> __sdk::Result<()> {
        self.enchant_item_then(target_slot, enchant_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `enchant_item` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn enchant_item_then(
        &self,
        target_slot: u8,
        enchant_id: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl enchant_item for super::RemoteReducers {
    fn enchant_item_then(
        &self,
        target_slot: u8,
        enchant_id: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            EnchantItemArgs {
                target_slot,
                enchant_id,
            },
            callback,
        )
    }
}
