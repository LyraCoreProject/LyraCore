// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SetGmLevelArgs {
    pub character_name: String,
    pub level: u8,
}

impl From<SetGmLevelArgs> for super::Reducer {
    fn from(args: SetGmLevelArgs) -> Self {
        Self::SetGmLevel {
            character_name: args.character_name,
            level: args.level,
        }
    }
}

impl __sdk::InModule for SetGmLevelArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `set_gm_level`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait set_gm_level {
    /// Request that the remote module invoke the reducer `set_gm_level` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`set_gm_level:set_gm_level_then`] to run a callback after the reducer completes.
    fn set_gm_level(&self, character_name: String, level: u8) -> __sdk::Result<()> {
        self.set_gm_level_then(character_name, level, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `set_gm_level` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn set_gm_level_then(
        &self,
        character_name: String,
        level: u8,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl set_gm_level for super::RemoteReducers {
    fn set_gm_level_then(
        &self,
        character_name: String,
        level: u8,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SetGmLevelArgs {
                character_name,
                level,
            },
            callback,
        )
    }
}
