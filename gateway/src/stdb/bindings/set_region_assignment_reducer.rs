// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SetRegionAssignmentArgs {
    pub map_id: u32,
    pub region_id: u32,
    pub shard: String,
    pub epoch: u64,
}

impl From<SetRegionAssignmentArgs> for super::Reducer {
    fn from(args: SetRegionAssignmentArgs) -> Self {
        Self::SetRegionAssignment {
            map_id: args.map_id,
            region_id: args.region_id,
            shard: args.shard,
            epoch: args.epoch,
        }
    }
}

impl __sdk::InModule for SetRegionAssignmentArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `set_region_assignment`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait set_region_assignment {
    /// Request that the remote module invoke the reducer `set_region_assignment` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`set_region_assignment:set_region_assignment_then`] to run a callback after the reducer completes.
    fn set_region_assignment(
        &self,
        map_id: u32,
        region_id: u32,
        shard: String,
        epoch: u64,
    ) -> __sdk::Result<()> {
        self.set_region_assignment_then(map_id, region_id, shard, epoch, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `set_region_assignment` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn set_region_assignment_then(
        &self,
        map_id: u32,
        region_id: u32,
        shard: String,
        epoch: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl set_region_assignment for super::RemoteReducers {
    fn set_region_assignment_then(
        &self,
        map_id: u32,
        region_id: u32,
        shard: String,
        epoch: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SetRegionAssignmentArgs {
                map_id,
                region_id,
                shard,
                epoch,
            },
            callback,
        )
    }
}
