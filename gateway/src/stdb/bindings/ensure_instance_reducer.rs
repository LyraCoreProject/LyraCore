// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct EnsureInstanceArgs {
    pub instance_id: u64,
    pub map_id: u32,
    pub party_id: u64,
}

impl From<EnsureInstanceArgs> for super::Reducer {
    fn from(args: EnsureInstanceArgs) -> Self {
        Self::EnsureInstance {
            instance_id: args.instance_id,
            map_id: args.map_id,
            party_id: args.party_id,
        }
    }
}

impl __sdk::InModule for EnsureInstanceArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `ensure_instance`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait ensure_instance {
    /// Request that the remote module invoke the reducer `ensure_instance` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`ensure_instance:ensure_instance_then`] to run a callback after the reducer completes.
    fn ensure_instance(&self, instance_id: u64, map_id: u32, party_id: u64) -> __sdk::Result<()> {
        self.ensure_instance_then(instance_id, map_id, party_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `ensure_instance` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn ensure_instance_then(
        &self,
        instance_id: u64,
        map_id: u32,
        party_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl ensure_instance for super::RemoteReducers {
    fn ensure_instance_then(
        &self,
        instance_id: u64,
        map_id: u32,
        party_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            EnsureInstanceArgs {
                instance_id,
                map_id,
                party_id,
            },
            callback,
        )
    }
}
