// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct TurnInQuestArgs {
    pub giver_guid: u64,
    pub quest_entry: u32,
    pub reward_index: u32,
}

impl From<TurnInQuestArgs> for super::Reducer {
    fn from(args: TurnInQuestArgs) -> Self {
        Self::TurnInQuest {
            giver_guid: args.giver_guid,
            quest_entry: args.quest_entry,
            reward_index: args.reward_index,
        }
    }
}

impl __sdk::InModule for TurnInQuestArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `turn_in_quest`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait turn_in_quest {
    /// Request that the remote module invoke the reducer `turn_in_quest` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`turn_in_quest:turn_in_quest_then`] to run a callback after the reducer completes.
    fn turn_in_quest(
        &self,
        giver_guid: u64,
        quest_entry: u32,
        reward_index: u32,
    ) -> __sdk::Result<()> {
        self.turn_in_quest_then(giver_guid, quest_entry, reward_index, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `turn_in_quest` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn turn_in_quest_then(
        &self,
        giver_guid: u64,
        quest_entry: u32,
        reward_index: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl turn_in_quest for super::RemoteReducers {
    fn turn_in_quest_then(
        &self,
        giver_guid: u64,
        quest_entry: u32,
        reward_index: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            TurnInQuestArgs {
                giver_guid,
                quest_entry,
                reward_index,
            },
            callback,
        )
    }
}
