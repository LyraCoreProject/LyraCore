// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::duel_type::Duel;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `game_duel`.
///
/// Obtain a handle from the [`GameDuelTableAccess::game_duel`] method on [`super::RemoteTables`],
/// like `ctx.db.game_duel()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_duel().on_insert(...)`.
pub struct GameDuelTableHandle<'ctx> {
    imp: __sdk::TableHandle<Duel>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

/// Lifetime-aware accessor marker for the table `game_duel`.
pub struct GameDuelTableAccessor;

impl __sdk::TableAccessor<super::RemoteTables> for GameDuelTableAccessor {
    type Row = Duel;
    type Handle<'db> = GameDuelTableHandle<'db>;

    fn get<'db>(db: &'db super::RemoteTables) -> Self::Handle<'db> {
        db.game_duel()
    }
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `game_duel`.
///
/// Implemented for [`super::RemoteTables`].
pub trait GameDuelTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`GameDuelTableHandle`], which mediates access to the table `game_duel`.
    fn game_duel(&self) -> GameDuelTableHandle<'_>;
}

impl GameDuelTableAccess for super::RemoteTables {
    fn game_duel(&self) -> GameDuelTableHandle<'_> {
        GameDuelTableHandle {
            imp: self.imp.get_table::<Duel>("game_duel"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct GameDuelInsertCallbackId(__sdk::CallbackId);
pub struct GameDuelDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableLike for GameDuelTableHandle<'ctx> {
    type Row = Duel;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = Duel> + '_ {
        self.imp.iter()
    }
}

impl<'ctx> __sdk::Table for GameDuelTableHandle<'ctx> {
    type Row = Duel;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = Duel> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = GameDuelInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameDuelInsertCallbackId {
        GameDuelInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameDuelInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = GameDuelDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameDuelDeleteCallbackId {
        GameDuelDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameDuelDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

impl<'ctx> __sdk::WithInsert for GameDuelTableHandle<'ctx> {
    type InsertCallbackId = GameDuelInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameDuelInsertCallbackId {
        GameDuelInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: GameDuelInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }
}

impl<'ctx> __sdk::WithDelete for GameDuelTableHandle<'ctx> {
    type DeleteCallbackId = GameDuelDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> GameDuelDeleteCallbackId {
        GameDuelDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: GameDuelDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct GameDuelUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for GameDuelTableHandle<'ctx> {
    type UpdateCallbackId = GameDuelUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameDuelUpdateCallbackId {
        GameDuelUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameDuelUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

impl<'ctx> __sdk::WithUpdate for GameDuelTableHandle<'ctx> {
    type UpdateCallbackId = GameDuelUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> GameDuelUpdateCallbackId {
        GameDuelUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: GameDuelUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `game_duel`,
/// which allows point queries on the field of the same name
/// via the [`GameDuelIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.game_duel().id().find(...)`.
pub struct GameDuelIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<Duel, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> GameDuelTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `game_duel`.
    pub fn id(&self) -> GameDuelIdUnique<'ctx> {
        GameDuelIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> GameDuelIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<Duel> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<Duel>("game_duel");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<Duel>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<Duel>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `Duel`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait game_duelQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `Duel`.
    fn game_duel(&self) -> __sdk::__query_builder::Table<Duel>;
}

impl game_duelQueryTableAccess for __sdk::QueryTableAccessor {
    fn game_duel(&self) -> __sdk::__query_builder::Table<Duel> {
        __sdk::__query_builder::Table::new("game_duel")
    }
}
