// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DebugRunRuntimeScriptArgs {
    pub script_name: String,
    pub event: String,
    pub source: String,
    pub actor_guid: u64,
    pub target_guid: u64,
}

impl From<DebugRunRuntimeScriptArgs> for super::Reducer {
    fn from(args: DebugRunRuntimeScriptArgs) -> Self {
        Self::DebugRunRuntimeScript {
            script_name: args.script_name,
            event: args.event,
            source: args.source,
            actor_guid: args.actor_guid,
            target_guid: args.target_guid,
        }
    }
}

impl __sdk::InModule for DebugRunRuntimeScriptArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `debug_run_runtime_script`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait debug_run_runtime_script {
    /// Request that the remote module invoke the reducer `debug_run_runtime_script` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`debug_run_runtime_script:debug_run_runtime_script_then`] to run a callback after the reducer completes.
    fn debug_run_runtime_script(
        &self,
        script_name: String,
        event: String,
        source: String,
        actor_guid: u64,
        target_guid: u64,
    ) -> __sdk::Result<()> {
        self.debug_run_runtime_script_then(
            script_name,
            event,
            source,
            actor_guid,
            target_guid,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `debug_run_runtime_script` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn debug_run_runtime_script_then(
        &self,
        script_name: String,
        event: String,
        source: String,
        actor_guid: u64,
        target_guid: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl debug_run_runtime_script for super::RemoteReducers {
    fn debug_run_runtime_script_then(
        &self,
        script_name: String,
        event: String,
        source: String,
        actor_guid: u64,
        target_guid: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DebugRunRuntimeScriptArgs {
                script_name,
                event,
                source,
                actor_guid,
                target_guid,
            },
            callback,
        )
    }
}
