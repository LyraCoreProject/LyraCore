// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RealmMailTakeMoneyFenceArgs {
    pub escrow_id: u64,
    pub payee_guid: u64,
    pub mail_id: u64,
    pub expect_money: u32,
}

impl From<RealmMailTakeMoneyFenceArgs> for super::Reducer {
    fn from(args: RealmMailTakeMoneyFenceArgs) -> Self {
        Self::RealmMailTakeMoneyFence {
            escrow_id: args.escrow_id,
            payee_guid: args.payee_guid,
            mail_id: args.mail_id,
            expect_money: args.expect_money,
        }
    }
}

impl __sdk::InModule for RealmMailTakeMoneyFenceArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `realm_mail_take_money_fence`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait realm_mail_take_money_fence {
    /// Request that the remote module invoke the reducer `realm_mail_take_money_fence` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`realm_mail_take_money_fence:realm_mail_take_money_fence_then`] to run a callback after the reducer completes.
    fn realm_mail_take_money_fence(
        &self,
        escrow_id: u64,
        payee_guid: u64,
        mail_id: u64,
        expect_money: u32,
    ) -> __sdk::Result<()> {
        self.realm_mail_take_money_fence_then(
            escrow_id,
            payee_guid,
            mail_id,
            expect_money,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `realm_mail_take_money_fence` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn realm_mail_take_money_fence_then(
        &self,
        escrow_id: u64,
        payee_guid: u64,
        mail_id: u64,
        expect_money: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl realm_mail_take_money_fence for super::RemoteReducers {
    fn realm_mail_take_money_fence_then(
        &self,
        escrow_id: u64,
        payee_guid: u64,
        mail_id: u64,
        expect_money: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RealmMailTakeMoneyFenceArgs {
                escrow_id,
                payee_guid,
                mail_id,
                expect_money,
            },
            callback,
        )
    }
}
