// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::session_actor_type::SessionActor;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ImportBotCharacterBlobArgs {
    pub transfer_id: u64,
    pub blob: Vec<u8>,
    pub source_module_identity: __sdk::Identity,
    pub intent_id: u64,
    pub controller_generation: u64,
    pub intent_created_micros: i64,
    pub source_map_id: u32,
    pub source_instance_id: u64,
    pub source_locator_revision: u64,
    pub request_actor: SessionActor,
}

impl From<ImportBotCharacterBlobArgs> for super::Reducer {
    fn from(args: ImportBotCharacterBlobArgs) -> Self {
        Self::ImportBotCharacterBlob {
            transfer_id: args.transfer_id,
            blob: args.blob,
            source_module_identity: args.source_module_identity,
            intent_id: args.intent_id,
            controller_generation: args.controller_generation,
            intent_created_micros: args.intent_created_micros,
            source_map_id: args.source_map_id,
            source_instance_id: args.source_instance_id,
            source_locator_revision: args.source_locator_revision,
            request_actor: args.request_actor,
        }
    }
}

impl __sdk::InModule for ImportBotCharacterBlobArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `import_bot_character_blob`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait import_bot_character_blob {
    /// Request that the remote module invoke the reducer `import_bot_character_blob` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`import_bot_character_blob:import_bot_character_blob_then`] to run a callback after the reducer completes.
    fn import_bot_character_blob(
        &self,
        transfer_id: u64,
        blob: Vec<u8>,
        source_module_identity: __sdk::Identity,
        intent_id: u64,
        controller_generation: u64,
        intent_created_micros: i64,
        source_map_id: u32,
        source_instance_id: u64,
        source_locator_revision: u64,
        request_actor: SessionActor,
    ) -> __sdk::Result<()> {
        self.import_bot_character_blob_then(
            transfer_id,
            blob,
            source_module_identity,
            intent_id,
            controller_generation,
            intent_created_micros,
            source_map_id,
            source_instance_id,
            source_locator_revision,
            request_actor,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `import_bot_character_blob` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn import_bot_character_blob_then(
        &self,
        transfer_id: u64,
        blob: Vec<u8>,
        source_module_identity: __sdk::Identity,
        intent_id: u64,
        controller_generation: u64,
        intent_created_micros: i64,
        source_map_id: u32,
        source_instance_id: u64,
        source_locator_revision: u64,
        request_actor: SessionActor,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl import_bot_character_blob for super::RemoteReducers {
    fn import_bot_character_blob_then(
        &self,
        transfer_id: u64,
        blob: Vec<u8>,
        source_module_identity: __sdk::Identity,
        intent_id: u64,
        controller_generation: u64,
        intent_created_micros: i64,
        source_map_id: u32,
        source_instance_id: u64,
        source_locator_revision: u64,
        request_actor: SessionActor,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ImportBotCharacterBlobArgs {
                transfer_id,
                blob,
                source_module_identity,
                intent_id,
                controller_generation,
                intent_created_micros,
                source_map_id,
                source_instance_id,
                source_locator_revision,
                request_actor,
            },
            callback,
        )
    }
}
