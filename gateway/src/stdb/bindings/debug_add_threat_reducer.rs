// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DebugAddThreatArgs {
    pub creature_guid: u64,
    pub source_guid: u64,
    pub amount: i64,
}

impl From<DebugAddThreatArgs> for super::Reducer {
    fn from(args: DebugAddThreatArgs) -> Self {
        Self::DebugAddThreat {
            creature_guid: args.creature_guid,
            source_guid: args.source_guid,
            amount: args.amount,
        }
    }
}

impl __sdk::InModule for DebugAddThreatArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `debug_add_threat`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait debug_add_threat {
    /// Request that the remote module invoke the reducer `debug_add_threat` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`debug_add_threat:debug_add_threat_then`] to run a callback after the reducer completes.
    fn debug_add_threat(
        &self,
        creature_guid: u64,
        source_guid: u64,
        amount: i64,
    ) -> __sdk::Result<()> {
        self.debug_add_threat_then(creature_guid, source_guid, amount, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `debug_add_threat` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn debug_add_threat_then(
        &self,
        creature_guid: u64,
        source_guid: u64,
        amount: i64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl debug_add_threat for super::RemoteReducers {
    fn debug_add_threat_then(
        &self,
        creature_guid: u64,
        source_guid: u64,
        amount: i64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DebugAddThreatArgs {
                creature_guid,
                source_guid,
                amount,
            },
            callback,
        )
    }
}
