// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct GwSkinArgs {
    pub actor_guid: u64,
    pub corpse_guid: u64,
}

impl From<GwSkinArgs> for super::Reducer {
    fn from(args: GwSkinArgs) -> Self {
        Self::GwSkin {
            actor_guid: args.actor_guid,
            corpse_guid: args.corpse_guid,
        }
    }
}

impl __sdk::InModule for GwSkinArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `gw_skin`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait gw_skin {
    /// Request that the remote module invoke the reducer `gw_skin` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`gw_skin:gw_skin_then`] to run a callback after the reducer completes.
    fn gw_skin(&self, actor_guid: u64, corpse_guid: u64) -> __sdk::Result<()> {
        self.gw_skin_then(actor_guid, corpse_guid, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `gw_skin` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn gw_skin_then(
        &self,
        actor_guid: u64,
        corpse_guid: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl gw_skin for super::RemoteReducers {
    fn gw_skin_then(
        &self,
        actor_guid: u64,
        corpse_guid: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            GwSkinArgs {
                actor_guid,
                corpse_guid,
            },
            callback,
        )
    }
}
