// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DebugNavProbeArgs {
    pub map: u32,
    pub x: f32,
    pub y: f32,
}

impl From<DebugNavProbeArgs> for super::Reducer {
    fn from(args: DebugNavProbeArgs) -> Self {
        Self::DebugNavProbe {
            map: args.map,
            x: args.x,
            y: args.y,
        }
    }
}

impl __sdk::InModule for DebugNavProbeArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `debug_nav_probe`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait debug_nav_probe {
    /// Request that the remote module invoke the reducer `debug_nav_probe` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`debug_nav_probe:debug_nav_probe_then`] to run a callback after the reducer completes.
    fn debug_nav_probe(&self, map: u32, x: f32, y: f32) -> __sdk::Result<()> {
        self.debug_nav_probe_then(map, x, y, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `debug_nav_probe` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn debug_nav_probe_then(
        &self,
        map: u32,
        x: f32,
        y: f32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl debug_nav_probe for super::RemoteReducers {
    fn debug_nav_probe_then(
        &self,
        map: u32,
        x: f32,
        y: f32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(DebugNavProbeArgs { map, x, y }, callback)
    }
}
