// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct GwEnchantItemArgs {
    pub actor_guid: u64,
    pub target_slot: u8,
    pub enchant_id: u32,
}

impl From<GwEnchantItemArgs> for super::Reducer {
    fn from(args: GwEnchantItemArgs) -> Self {
        Self::GwEnchantItem {
            actor_guid: args.actor_guid,
            target_slot: args.target_slot,
            enchant_id: args.enchant_id,
        }
    }
}

impl __sdk::InModule for GwEnchantItemArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `gw_enchant_item`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait gw_enchant_item {
    /// Request that the remote module invoke the reducer `gw_enchant_item` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`gw_enchant_item:gw_enchant_item_then`] to run a callback after the reducer completes.
    fn gw_enchant_item(
        &self,
        actor_guid: u64,
        target_slot: u8,
        enchant_id: u32,
    ) -> __sdk::Result<()> {
        self.gw_enchant_item_then(actor_guid, target_slot, enchant_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `gw_enchant_item` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn gw_enchant_item_then(
        &self,
        actor_guid: u64,
        target_slot: u8,
        enchant_id: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl gw_enchant_item for super::RemoteReducers {
    fn gw_enchant_item_then(
        &self,
        actor_guid: u64,
        target_slot: u8,
        enchant_id: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            GwEnchantItemArgs {
                actor_guid,
                target_slot,
                enchant_id,
            },
            callback,
        )
    }
}
