// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct PetCommandArgs {
    pub data: u32,
    pub target_guid: u64,
}

impl From<PetCommandArgs> for super::Reducer {
    fn from(args: PetCommandArgs) -> Self {
        Self::PetCommand {
            data: args.data,
            target_guid: args.target_guid,
        }
    }
}

impl __sdk::InModule for PetCommandArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `pet_command`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait pet_command {
    /// Request that the remote module invoke the reducer `pet_command` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`pet_command:pet_command_then`] to run a callback after the reducer completes.
    fn pet_command(&self, data: u32, target_guid: u64) -> __sdk::Result<()> {
        self.pet_command_then(data, target_guid, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `pet_command` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn pet_command_then(
        &self,
        data: u32,
        target_guid: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl pet_command for super::RemoteReducers {
    fn pet_command_then(
        &self,
        data: u32,
        target_guid: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(PetCommandArgs { data, target_guid }, callback)
    }
}
