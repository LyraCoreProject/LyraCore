// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DebugEncounterSpawnWaveArgs {
    pub instance_id: u64,
    pub encounter_id: u32,
    pub map_id: u32,
    pub entry: u32,
    pub count: u32,
    pub x: f32,
    pub y: f32,
    pub z: f32,
    pub orientation: f32,
}

impl From<DebugEncounterSpawnWaveArgs> for super::Reducer {
    fn from(args: DebugEncounterSpawnWaveArgs) -> Self {
        Self::DebugEncounterSpawnWave {
            instance_id: args.instance_id,
            encounter_id: args.encounter_id,
            map_id: args.map_id,
            entry: args.entry,
            count: args.count,
            x: args.x,
            y: args.y,
            z: args.z,
            orientation: args.orientation,
        }
    }
}

impl __sdk::InModule for DebugEncounterSpawnWaveArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `debug_encounter_spawn_wave`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait debug_encounter_spawn_wave {
    /// Request that the remote module invoke the reducer `debug_encounter_spawn_wave` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`debug_encounter_spawn_wave:debug_encounter_spawn_wave_then`] to run a callback after the reducer completes.
    fn debug_encounter_spawn_wave(
        &self,
        instance_id: u64,
        encounter_id: u32,
        map_id: u32,
        entry: u32,
        count: u32,
        x: f32,
        y: f32,
        z: f32,
        orientation: f32,
    ) -> __sdk::Result<()> {
        self.debug_encounter_spawn_wave_then(
            instance_id,
            encounter_id,
            map_id,
            entry,
            count,
            x,
            y,
            z,
            orientation,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `debug_encounter_spawn_wave` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn debug_encounter_spawn_wave_then(
        &self,
        instance_id: u64,
        encounter_id: u32,
        map_id: u32,
        entry: u32,
        count: u32,
        x: f32,
        y: f32,
        z: f32,
        orientation: f32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl debug_encounter_spawn_wave for super::RemoteReducers {
    fn debug_encounter_spawn_wave_then(
        &self,
        instance_id: u64,
        encounter_id: u32,
        map_id: u32,
        entry: u32,
        count: u32,
        x: f32,
        y: f32,
        z: f32,
        orientation: f32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DebugEncounterSpawnWaveArgs {
                instance_id,
                encounter_id,
                map_id,
                entry,
                count,
                x,
                y,
                z,
                orientation,
            },
            callback,
        )
    }
}
