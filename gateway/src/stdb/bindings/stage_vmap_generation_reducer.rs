// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct StageVmapGenerationArgs {
    pub generation_id: u64,
    pub map_id: u32,
    pub expected_chunks: u32,
    pub expected_bytes: u64,
    pub manifest_digest_hex: String,
    pub source_identity: String,
    pub selection_identity: String,
}

impl From<StageVmapGenerationArgs> for super::Reducer {
    fn from(args: StageVmapGenerationArgs) -> Self {
        Self::StageVmapGeneration {
            generation_id: args.generation_id,
            map_id: args.map_id,
            expected_chunks: args.expected_chunks,
            expected_bytes: args.expected_bytes,
            manifest_digest_hex: args.manifest_digest_hex,
            source_identity: args.source_identity,
            selection_identity: args.selection_identity,
        }
    }
}

impl __sdk::InModule for StageVmapGenerationArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `stage_vmap_generation`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait stage_vmap_generation {
    /// Request that the remote module invoke the reducer `stage_vmap_generation` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`stage_vmap_generation:stage_vmap_generation_then`] to run a callback after the reducer completes.
    fn stage_vmap_generation(
        &self,
        generation_id: u64,
        map_id: u32,
        expected_chunks: u32,
        expected_bytes: u64,
        manifest_digest_hex: String,
        source_identity: String,
        selection_identity: String,
    ) -> __sdk::Result<()> {
        self.stage_vmap_generation_then(
            generation_id,
            map_id,
            expected_chunks,
            expected_bytes,
            manifest_digest_hex,
            source_identity,
            selection_identity,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `stage_vmap_generation` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn stage_vmap_generation_then(
        &self,
        generation_id: u64,
        map_id: u32,
        expected_chunks: u32,
        expected_bytes: u64,
        manifest_digest_hex: String,
        source_identity: String,
        selection_identity: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl stage_vmap_generation for super::RemoteReducers {
    fn stage_vmap_generation_then(
        &self,
        generation_id: u64,
        map_id: u32,
        expected_chunks: u32,
        expected_bytes: u64,
        manifest_digest_hex: String,
        source_identity: String,
        selection_identity: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            StageVmapGenerationArgs {
                generation_id,
                map_id,
                expected_chunks,
                expected_bytes,
                manifest_digest_hex,
                source_identity,
                selection_identity,
            },
            callback,
        )
    }
}
