// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RealmGroupOpArgs {
    pub op: u8,
    pub actor_guid: u64,
    pub target_guid: u64,
    pub arg_a: u8,
    pub arg_b: u8,
}

impl From<RealmGroupOpArgs> for super::Reducer {
    fn from(args: RealmGroupOpArgs) -> Self {
        Self::RealmGroupOp {
            op: args.op,
            actor_guid: args.actor_guid,
            target_guid: args.target_guid,
            arg_a: args.arg_a,
            arg_b: args.arg_b,
        }
    }
}

impl __sdk::InModule for RealmGroupOpArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `realm_group_op`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait realm_group_op {
    /// Request that the remote module invoke the reducer `realm_group_op` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`realm_group_op:realm_group_op_then`] to run a callback after the reducer completes.
    fn realm_group_op(
        &self,
        op: u8,
        actor_guid: u64,
        target_guid: u64,
        arg_a: u8,
        arg_b: u8,
    ) -> __sdk::Result<()> {
        self.realm_group_op_then(op, actor_guid, target_guid, arg_a, arg_b, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `realm_group_op` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn realm_group_op_then(
        &self,
        op: u8,
        actor_guid: u64,
        target_guid: u64,
        arg_a: u8,
        arg_b: u8,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl realm_group_op for super::RemoteReducers {
    fn realm_group_op_then(
        &self,
        op: u8,
        actor_guid: u64,
        target_guid: u64,
        arg_a: u8,
        arg_b: u8,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RealmGroupOpArgs {
                op,
                actor_guid,
                target_guid,
                arg_a,
                arg_b,
            },
            callback,
        )
    }
}
