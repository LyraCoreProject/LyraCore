// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SetPackageConfigArgs {
    pub package_name: String,
    pub key: String,
    pub value: String,
    pub allow_new: bool,
}

impl From<SetPackageConfigArgs> for super::Reducer {
    fn from(args: SetPackageConfigArgs) -> Self {
        Self::SetPackageConfig {
            package_name: args.package_name,
            key: args.key,
            value: args.value,
            allow_new: args.allow_new,
        }
    }
}

impl __sdk::InModule for SetPackageConfigArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `set_package_config`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait set_package_config {
    /// Request that the remote module invoke the reducer `set_package_config` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`set_package_config:set_package_config_then`] to run a callback after the reducer completes.
    fn set_package_config(
        &self,
        package_name: String,
        key: String,
        value: String,
        allow_new: bool,
    ) -> __sdk::Result<()> {
        self.set_package_config_then(package_name, key, value, allow_new, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `set_package_config` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn set_package_config_then(
        &self,
        package_name: String,
        key: String,
        value: String,
        allow_new: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl set_package_config for super::RemoteReducers {
    fn set_package_config_then(
        &self,
        package_name: String,
        key: String,
        value: String,
        allow_new: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SetPackageConfigArgs {
                package_name,
                key,
                value,
                allow_new,
            },
            callback,
        )
    }
}
