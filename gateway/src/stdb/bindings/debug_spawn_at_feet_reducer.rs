// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DebugSpawnAtFeetArgs {
    pub character_guid: u64,
    pub creature_entry: u32,
    pub offset: f32,
}

impl From<DebugSpawnAtFeetArgs> for super::Reducer {
    fn from(args: DebugSpawnAtFeetArgs) -> Self {
        Self::DebugSpawnAtFeet {
            character_guid: args.character_guid,
            creature_entry: args.creature_entry,
            offset: args.offset,
        }
    }
}

impl __sdk::InModule for DebugSpawnAtFeetArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `debug_spawn_at_feet`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait debug_spawn_at_feet {
    /// Request that the remote module invoke the reducer `debug_spawn_at_feet` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`debug_spawn_at_feet:debug_spawn_at_feet_then`] to run a callback after the reducer completes.
    fn debug_spawn_at_feet(
        &self,
        character_guid: u64,
        creature_entry: u32,
        offset: f32,
    ) -> __sdk::Result<()> {
        self.debug_spawn_at_feet_then(character_guid, creature_entry, offset, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `debug_spawn_at_feet` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn debug_spawn_at_feet_then(
        &self,
        character_guid: u64,
        creature_entry: u32,
        offset: f32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl debug_spawn_at_feet for super::RemoteReducers {
    fn debug_spawn_at_feet_then(
        &self,
        character_guid: u64,
        creature_entry: u32,
        offset: f32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DebugSpawnAtFeetArgs {
                character_guid,
                creature_entry,
                offset,
            },
            callback,
        )
    }
}
