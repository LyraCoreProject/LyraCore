// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::session_actor_type::SessionActor;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RealmAuctionDecideCancelArgs {
    pub operation_id: u64,
    pub request_actor: SessionActor,
    pub auction_id: u32,
    pub house: u32,
    pub cut: u32,
}

impl From<RealmAuctionDecideCancelArgs> for super::Reducer {
    fn from(args: RealmAuctionDecideCancelArgs) -> Self {
        Self::RealmAuctionDecideCancel {
            operation_id: args.operation_id,
            request_actor: args.request_actor,
            auction_id: args.auction_id,
            house: args.house,
            cut: args.cut,
        }
    }
}

impl __sdk::InModule for RealmAuctionDecideCancelArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `realm_auction_decide_cancel`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait realm_auction_decide_cancel {
    /// Request that the remote module invoke the reducer `realm_auction_decide_cancel` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`realm_auction_decide_cancel:realm_auction_decide_cancel_then`] to run a callback after the reducer completes.
    fn realm_auction_decide_cancel(
        &self,
        operation_id: u64,
        request_actor: SessionActor,
        auction_id: u32,
        house: u32,
        cut: u32,
    ) -> __sdk::Result<()> {
        self.realm_auction_decide_cancel_then(
            operation_id,
            request_actor,
            auction_id,
            house,
            cut,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `realm_auction_decide_cancel` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn realm_auction_decide_cancel_then(
        &self,
        operation_id: u64,
        request_actor: SessionActor,
        auction_id: u32,
        house: u32,
        cut: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl realm_auction_decide_cancel for super::RemoteReducers {
    fn realm_auction_decide_cancel_then(
        &self,
        operation_id: u64,
        request_actor: SessionActor,
        auction_id: u32,
        house: u32,
        cut: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RealmAuctionDecideCancelArgs {
                operation_id,
                request_actor,
                auction_id,
                house,
                cut,
            },
            callback,
        )
    }
}
