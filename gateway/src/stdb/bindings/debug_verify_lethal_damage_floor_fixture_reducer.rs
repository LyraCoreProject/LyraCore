// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DebugVerifyLethalDamageFloorFixtureArgs {
    pub creature_guid: u64,
    pub expected_health: u32,
    pub expected_dead: bool,
    pub expected_floor: bool,
    pub expected_notification: bool,
    pub expected_absorb: i32,
}

impl From<DebugVerifyLethalDamageFloorFixtureArgs> for super::Reducer {
    fn from(args: DebugVerifyLethalDamageFloorFixtureArgs) -> Self {
        Self::DebugVerifyLethalDamageFloorFixture {
            creature_guid: args.creature_guid,
            expected_health: args.expected_health,
            expected_dead: args.expected_dead,
            expected_floor: args.expected_floor,
            expected_notification: args.expected_notification,
            expected_absorb: args.expected_absorb,
        }
    }
}

impl __sdk::InModule for DebugVerifyLethalDamageFloorFixtureArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `debug_verify_lethal_damage_floor_fixture`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait debug_verify_lethal_damage_floor_fixture {
    /// Request that the remote module invoke the reducer `debug_verify_lethal_damage_floor_fixture` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`debug_verify_lethal_damage_floor_fixture:debug_verify_lethal_damage_floor_fixture_then`] to run a callback after the reducer completes.
    fn debug_verify_lethal_damage_floor_fixture(
        &self,
        creature_guid: u64,
        expected_health: u32,
        expected_dead: bool,
        expected_floor: bool,
        expected_notification: bool,
        expected_absorb: i32,
    ) -> __sdk::Result<()> {
        self.debug_verify_lethal_damage_floor_fixture_then(
            creature_guid,
            expected_health,
            expected_dead,
            expected_floor,
            expected_notification,
            expected_absorb,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `debug_verify_lethal_damage_floor_fixture` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn debug_verify_lethal_damage_floor_fixture_then(
        &self,
        creature_guid: u64,
        expected_health: u32,
        expected_dead: bool,
        expected_floor: bool,
        expected_notification: bool,
        expected_absorb: i32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl debug_verify_lethal_damage_floor_fixture for super::RemoteReducers {
    fn debug_verify_lethal_damage_floor_fixture_then(
        &self,
        creature_guid: u64,
        expected_health: u32,
        expected_dead: bool,
        expected_floor: bool,
        expected_notification: bool,
        expected_absorb: i32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DebugVerifyLethalDamageFloorFixtureArgs {
                creature_guid,
                expected_health,
                expected_dead,
                expected_floor,
                expected_notification,
                expected_absorb,
            },
            callback,
        )
    }
}
